# LabelHub · 开发日志 / CHANGELOG

> 此文档记录 LabelHub 从 Sprint 0 到 Sprint 7 的逐次代码改动、技术决策与验证记录。评委可阅读本文档了解项目的迭代过程与工程纪律。
> 评委门面快速链接:[README](../README.md) · [架构](ARCHITECTURE.md) · [部署](DEPLOY.md) · [API](openapi.yaml) · [Code Review](CODE-REVIEW-FINAL.md)

> **维护约定**:每次代码改动后,在「最近完成」追加一条,按需更新「仍需提升 / 下一步 / 验证记录」,并同步 `CLAUDE.md` / `CODEX.md`。每条结构:
> - **本次完成**:做了哪些功能、修复或重构
> - **仍需提升**:已知风险、体验问题、技术债或 warning
> - **下一步**:下一轮最应该推进的任务
> - **验证记录**:已跑过的测试、lint、build 或手动验证

## 最近完成

- 2026-05-31 `owner-left-nav-three-groups`: 把上一条 `owner-left-nav-sections` 的单一「视图」导航(数据看板/AI 预审/数据导出 3 项)重排成对齐组织方参考稿 `~/labelhub-ui-demo/src/components/SideNav.tsx` 的**三组结构**(数据生产 / 审核与质检 / 数据交付),解决「任务管理页太长」并贴合 demo 左栏。**只动** `apps/web/src/modules/owner/Dashboard.tsx`,不动后端/其它模块/逻辑。① **方案(路线 A,经用户拍板)**:demo 的 SideNav 是「数据生产(任务管理/模板搭建/数据集)· 审核与质检(AI 预审规则/人工审核)· 数据交付(数据看板/导出中心)」,其中 AI 只有**一个**导航项(黄金评测本就折叠在「AI 预审规则」内)。本项目据此把 AI 配置 + 黄金评测合为单一「AI 预审」分节(与 demo 一致),并按 RBAC 把 demo 的「人工审核」替换为 Owner 的「**审核结果(只读)**」——人工审核的初/复/终审**动作**留在 Reviewer 角色,Owner 不审、只看聚合(这是与单人 walkthrough demo 的关键差异,守住权限模型)。② **结构**:`detailSection` 由 `'stats'|'ai'|'export'` 扩为 `'template'|'dataset'|'ai'|'review'|'stats'|'export'`(默认仍 `'ai'`);新增模块级 `SIDE_NAV_GROUPS` 常量(三组、6 个可切换项,带 `DetailSection` 类型约束);左栏把原单个 `lh-side-section` 换成三组 `lh-side-section`(各带组标题,沿用 `lh-side-item` 皮肤 + `aria-pressed` + `aria-label="视图 X"`),仅选中任务时显示。**任务管理 = 左栏常驻的任务列表**(选任务入口,位置不动),故不单列为第 7 个切换项。③ **新增 3 个右栏分节**(与既有 ai/stats/export 同样用 `selected` 守卫,不改守卫结构):`'template'`→说明 + 「打开 Designer」链接(沿用既有 `templateDesignerLinkStyle`/路由,不内嵌 Designer);`'dataset'`→直接复用既有 `ImportPanel`(导入 JSON/JSONL/Excel + 批量编辑);`'review'`→只读聚合面板,用既有 state(`finishedItems/totalItems`、`aiReviewEnabled`)经 `MetricCell` 展示进度 + AI 开关 + 文案化「人工审核动作在 Reviewer / 详细图表见数据看板」,**不加新 fetch/端点**。④ **零逻辑改动**:所有 state/handlers/effects/序号守护/API 调用/`selectTask`/自动选中首题(`data.find(taskId) ?? data[0]`)/深链(`?taskId&aiPromptId#ai-prompts` 仍落默认 AI 节)一字未动;保留精确 label `视图 AI 预审`/`视图 数据导出`,默认 `'ai'` 使既有测试渲染后即见 AI 控件——**37 个 Dashboard 测试零改动全过**。仍需提升:① 「任务管理」仍是左栏常驻列表而非独立第 7 导航项(零测试改动 + 保留选任务入口的折中,demo 三组视觉已具备);② 「审核结果」为聚合只读,逐条审核结果列表需新增 owner 端点(Reviewer 的 `/reviewer/results` 权限不同不可直接复用),列为后续;③ 视图导航仍在 Owner 页内左列而非全局 `AppLayout` 侧栏(跨角色共享 + 独立测试,本次按范围不动)。验证:`pnpm -F web exec tsc -b` exit 0;`pnpm -F web exec vitest run src/modules/owner/Dashboard.test.tsx` 37/37 通过(零改动);`src/modules/owner` 全量 56/56 通过;`pnpm -F web build` exit 0(仅既有 StatsBoard chunk-size warning)。备注:本机 vitest forks pool 偶发 worker 启动超时(基础设施问题,非测试失败),`--pool=threads` 稳定全绿。

- 2026-05-30 `fix-ci-integration-multilevel`: 修复 PR CI 的 `integration` job(testcontainers,`-tags=integration`)失败——`TestMainFlowWithRealMySQLRedis` 仍按"单次 approve→approved"断言,而 `reviewer-multilevel-review` 之后单次 approve 只 advance 一级(停留 human_reviewing),定稿需 3 次(初审/复审/终审)。该改动当时更新了 handler 单测但漏了这个 testcontainers 集成测试。① 把单次 approve 改成 `for level := 1..RequiredHumanReviewLevels` 三次 approve,前两次断言仍 `human_reviewing`、第三次 `assertApproved`。② 给集成测试的 MySQL 连接串加 `clientFoundRows=true`:`review.Apply` 中间级 approve 用 `UPDATE ... SET updated_at=now WHERE status='human_reviewing'` + `RowsAffected==1` 做乐观守卫,快测里两次 approve 落在同一秒导致 updated_at 不变,MySQL 默认的"变更行数"语义读成 0 而误报 `ErrConcurrentWrite`;"匹配行数"才是该守卫本意(生产中两次人审间隔数秒,不触发)。只动 `internal/integration/main_flow_integration_test.go`,不改生产逻辑/DSN。验证:本地 Colima(`DOCKER_HOST=unix://~/.colima/default/docker.sock`,`TESTCONTAINERS_RYUK_DISABLED=true`)跑 `go test -tags=integration ./internal/integration -run TestMainFlowWithRealMySQLRedis` 通过(7.1s);`go vet -tags=integration ./internal/integration` 无问题。备注:生产 baseDSN 未加 clientFoundRows(避免影响全局其它依赖"变更行数"的幂等/乐观检查),如需对中间级 approve 更稳健可后续单独评估。

- 2026-05-30 `owner-left-nav-sections`: 按组织方参考稿 `~/labelhub-ui-demo` 的左侧 `SideNav` 模式,把 Owner 任务详情区从「一长条竖向堆叠」改成**左栏可切换分节 + 主区一次只显一节**,解决「任务管理页太长」。**只动** `apps/web/src/modules/owner/Dashboard.tsx`(+ 同名测试),不动后端/其它模块。① **结构**:新增 `detailSection` 状态(`'stats'|'ai'|'export'`,默认 `'ai'`);在左栏(原任务列表所在列)任务列表下方加一个 `lh-side-section` 视图导航(数据看板 / AI 预审 / 数据导出,复用既有 `lh-side-item` 皮肤,带 `aria-pressed`/`aria-label="视图 X"`),仅在选中任务时显示;右栏把原本顺序堆叠的 `StatsBoard`(看板)、`ExportPanel`(导出)、`Baseline + <section id="ai-prompts">`(AI 预审:基线/Prompt/维度/阈值/模型/Dry-run/Golden/历史)三块各自用 `{detailSection === 'x' && ...}` 包起来,一次只渲染一节。任务头部(标题/导出数据/模板 Designer 链接)与指标条 MetricCell 仍常驻在分节之上。② **与参考稿对齐**:ui-demo SideNav 把「AI 预审规则」作为**单个**导航项(不再细分配置/评测),故本次 AI 配置 + 评测合为一节「AI 预审」,与图一致;「任务管理」即左栏常驻的任务列表(选任务在此),「模板搭建」沿用既有 Designer 路由(头部链接进入)。③ **默认 `'ai'` 的原因**:选中任务后直接进入 AI 预审(本项目核心能力),且保证既有 Dashboard 测试(全部围绕 baseline/prompt/dry-run/golden/history)在渲染后即可见 AI 控件、任务列表常驻可切任务——**36 个既有测试零改动全过**。④ **逻辑零改**:所有 state/handlers/effects/序号守护/API 调用/`selectTask`/深链(`?taskId&aiPromptId#ai-prompts` 仍落在默认 AI 节)一字未动,仅重排 JSX + 加分节开关。仍需提升:视图导航放在 Owner 页左列(任务列表同列)而非 AppLayout 外层全局侧栏(因详情分节与任务选择需共存,且外层侧栏为跨角色共享、单独测试);若要把分节项提升到外层侧栏可后续做。验证:`pnpm -F web exec tsc -b` exit 0;`pnpm -F web exec vitest run src/modules/owner/Dashboard.test.tsx` 37 通过(36 既有 + 新增「左栏视图分节切换 AI/导出」一条:默认见 prompt_template → 切数据导出 AI 控件卸载且出现导出面板 → 切回 AI 重新可见);`src/modules/owner` 全量 56 通过;`pnpm -F web build` exit 0(仅既有 StatsBoard chunk-size warning)。

- 2026-05-30 `handoff-followups-4321`: 收口 `docs/HANDOFF.md` 列出的 4 项遗留(按 4→3→2→1)。① **提交文档登录信息修正(HANDOFF #4)**:`submission/README.md` + `submission/DEMO_SCRIPT.md` 把登录密码 `pass` 改正为 seed 实际的 `123456`(核实 `cmd/seed/main.go:27 demoPassword="123456"`),且只改作登录凭据的 `pass`,保留 AI verdict 语义的 `pass/reject/uncertain`;README 评估账号表里 `admin` 用户名一并改正为 seed 实际的 `admin1`。`DEPLOY.md`/`DEMO_ENV.md` 里的 `admin`/`pass` 系 asynqmon basic-auth 语境,不动;`CODE-REVIEW-FINAL.md` 的 `pass` 是历史审查快照,保留。② **Owner 批量编辑列表端点(HANDOFF #2)**:后端新增 `GET /tasks/:taskId/items`(owner/admin,`handler/task.go ListItems`),按 id 升序游标分页(`?limit=` 默认20/上限100、`?cursor=` 为上页末条 id,多取一条判 `has_more` 并回 `next_cursor`),返回每条 `{id,externalId,status,priority,payload}`,经 `loadOwnedTask` 校验归属;前端 `client.ts` 加 `listTaskItems`(因 PageOK 的 `page` 被标准 `apiGet` 丢弃,直接读 envelope 保留游标),`ImportPanel.tsx` 把原来「逐条 item-preview 加一题」改为「加载题目列表」一次拉全任务题目进可编辑列表 + `加载更多` 翻页 + 行内展示状态,随机预览保留。③ **Owner 旧面板换皮统一(HANDOFF #1)**:把仍用 Editorial 设计令牌的 3 个 Owner 旧面板(`Dashboard.tsx` 的 AI Prompt/golden-sample/dry-run/baseline/列表 + `ExportPanel.tsx` + `StatsBoard.tsx`)的内联样式令牌做**限定在这 3 个文件**的 Editorial→lh 重映射:`--color-accent`(暖红 #b8442e)→`--lh-primary`(蓝 #165dff)、`--font-heading`(Georgia 衬线)→`--lh-font-sans`(系统无衬线)、暖灰 `--color-text/-secondary/-muted/-bg/-border*/-surface*` → 冷灰 `--lh-text-1/2/3/-bg/-border*/-bg-card/-bg-elev`、`--color-success/danger/teal` → `--lh-success/danger/cyan`;空间/圆角/阴影/字号(`--space/-radius/-shadow/-text-*`)系中性尺寸,保持不变。**未动全局 `tokens.css`**(Editorial 令牌仍被另 22 个文件使用,全局换会波及已定稿的 Designer/Reviewer/Login/Renderer,超出范围),仅替换这 3 个文件内的令牌引用;JSX 结构与所有 hooks/handlers/API 调用零改动,故 61KB 的 `Dashboard.test.tsx` 等行为测试不受影响。仍需提升:换皮为令牌级统一(色彩+字体),卡片内边距/标题层级未逐一改成 `lh-card__head/body`(属更细打磨)。④ **部署 + 生产浏览器验证**:rsync 工作树 → `docker compose up -d --build web api worker` 重建上线 http://43.155.210.70(curl 200,三容器新起)。chrome-devtools 冒烟逐项确认:**HANDOFF #2** `GET /tasks/1/items?limit=3` 返回 3 条 + `page{next_cursor:'3',has_more:true}` + 状态映射;**HANDOFF #1** 换皮——Owner 仪表盘「保存配置」按钮/滑块/StatsBoard「7/30」「77.8%」/进度条/「模板 Designer」按钮全部呈 lh 蓝 #165dff、标题系统无衬线、面板冷灰(实测 h1 字体已从 Georgia 衬线变 `--lh-font-sans`,`--lh-primary=#165dff` 生效),5 个旧面板齐备;**HANDOFF #3** Designer 端到端——经 API 建临时任务#3+条件模板(Radio `decision`{approve,reject} + Input `reason`,`visibleWhen{decision==reject}` + `customRule{len(value)>=5}`)→ owner Designer 联动/校验 tab 正确回显 visibleWhen(控制字段 decision、equals reject)与 customRule(expr/message)→ labeler1 领题作答:`reason` 初始隐藏、选 reject 后出现(visibleWhen 生效)、填 2 字提交被「打回理由至少需要 5 个字符」拦下(customRule 生效)、填 8 字提交成功(进度 100%);labeler 页 console 无 error/warn。验证后临时任务#3 已 `pause`(移出标注广场)。⑤ **顺带发现一处既有缺陷(非本批引入,已上报)**:`tasks.status` 列 ENUM 为 `('draft','published','paused','archived')`(`migration/001_initial_schema.up.sql:45`),而任务状态机 `statemachine/task.go` 终态用 `'ended'`,导致 Owner「下线/end」`UPDATE status='ended'` 触发 MySQL `Error 1265 Data truncated`——线上「下线」按钮对任意任务都会失败。**已修复并部署**:新增 migration `008_task_status_add_ended`(`ALTER TABLE tasks MODIFY status ENUM('draft','published','paused','archived','ended')`,保留 archived 向后兼容;down 先把 ended 退回 paused 再收缩),rsync + 重建 api(启动自动跑迁移)上线,验证 `POST /tasks/3/end` 返回 `status=ended`(下线恢复正常)。验证用临时任务 #3 随后从生产库事务删除(`DELETE FROM submissions WHERE task_id=3` → `DELETE FROM tasks WHERE id=3`,子表经 CASCADE 清理,outbox/audit 无 FK 不阻断),owner 任务列表恢复为 2 个官方任务。验证:`go build ./...` 通过;`go vet ./internal/handler/` 无问题;`go test ./internal/handler/` 153 通过(含新增 `TestListItemsReturnsTaskItemRows`/`TestListItemsPaginatesWithCursor`);`pnpm -F web exec tsc -b` exit 0;`pnpm -F web exec vitest run src/modules/owner` 55 通过、`src/shared/api/client.test.ts` 通过(新增 `listTaskItems` 两条:limit/page-envelope 映射、cursor 透传+`has_more` 缺省 false);`pnpm -F web build` exit 0(仅既有 StatsBoard chunk-size warning);grep 确认 3 个旧面板内已无 `--color-*`/`--font-*` Editorial 令牌残留。

- 2026-05-30 `reviewer-console-reskin`: 把 Reviewer 控制台**前端**改皮到组织方参考视觉(`~/labelhub-ui-demo` HumanReview 三栏 + AiReview AI 评审块),并接通多级审核阶段展示与审核结果列表。**只动** `apps/web/src/modules/reviewer/**` + `src/shared/api/client.ts`,`renderer/`/`modules/labeler/`/`modules/owner/`/`modules/template/` 零改动。① **client**:`Submission`/`TaskBundle` 加 `reviewStage`('first'|'second'|'final')/`reviewLevel`(1|2|3)/`requiredLevels`(3);新增 `ReviewStage`/`ReviewResult`/`ReviewResultsPage` 类型与 `listReviewResults({cursor,limit})` wrapper——因后端 `/reviewer/results` 用 PageOK 返回 `{data, page:{next_cursor,has_more}}` 而标准 `apiGet` 只解 `data`,wrapper 直接读 envelope 保留游标。② **组件结构**:新增 `stage.ts`(`resolveStage` 把扁平 stage 字段→{label 初审/复审/终审, level, required, progress},缺失退回 first 1/3)、`AIVerdictPanel.tsx`(AiReview 风格的 AI 预审块:真实 `aiReview.dimensions` 渲染 `.ai-score-row` ScoreBar[安全维度低分红/其余低分橙/达标绿]、verdict pill、评语、tokens/latency,数据全来自真实 aiReview/submission)、`ReviewResults.tsx`(消费 `listReviewResults`、`.ai-section` 列表、`加载更多` 游标分页、点击行回调打开只读详情)。③ **Queue.tsx 改皮**:`hr-shell` 三栏[左队列 `.hr-item` + filter tab + `.hr-batch` 批量条;中 `.hr-main` 详情用既有 `SchemaRenderer readOnly` + `AIVerdictPanel` + `.hr-decisions` 决策按钮 + `.hr-textarea`+`.hr-quick-tags` 审核意见;右 `.hr-right` 统计 + 审计时间线];顶部加「审核工作台 / 审核结果」视图 tab(`.hr-side__tabs`),results 视图挂 `ReviewResults`,点击结果行 `openResultDetail` 包装成 real submission 复用 `openQueueItem` 加载只读详情并切回工作台。④ **阶段展示**:队列行加 `.hr-tag--purple` 阶段小徽标(初审/复审/终审 + 进度),详情头部显示「复审 · 审核进度 2/3」,`.hr-decisions` 的通过按钮文案带阶段「✓ 通过(复审)」+ hint「推进一级 · 审核进度 2/3」,体现 approve 推进一级。⑤ **逻辑零改**:`loadQueue`/`openQueueItem`/`review`/`batchReview`/`retryAIReview`/`openRuleConfig` 全部 handler、`ruleLoadSeq`/`detailLoadSeq` 加载序号守护、所有 API 调用(`/reviewer/submissions`、`/reviewer/submissions/:id`、`/submissions/:id/review`、`/reviews/batch`、`/ai-review/retry`、`/ai-prompts`)、`SchemaRenderer` 用法、demo 兜底数据一字未动;仅替换 markup class + 抽组件 + 加阶段/结果视图。删除改皮后孤立的内联 style 常量(`leftPanelStyle`/`decisionGridStyle` 等)与重复的 `normalizeDimensions`。仍需提升:results 视图未做按 status/stage 过滤(后端也未提供);阶段仅顶层 submission 维度;legacy Semi 通过/打回/拒绝 按钮保持纯文案(测试锚点),阶段文案落在 `.hr-decision` 突出按钮上。验证:`pnpm -F web exec vitest run src/modules/reviewer` 11/11 通过(9 既有行为断言全保留 + 新增「审核结果列表渲染并打开只读详情」「复审阶段徽标 + 审核进度 2/3 + 通过(复审) 文案」2 条;`Queue.test.tsx` mock 补 reviewStage/reviewLevel/requiredLevels 字段 + `listReviewResults` mock);全量 `pnpm -F web exec vitest run` 155/155 通过;`pnpm -F web exec tsc -b` exit 0;`pnpm -F web lint` clean;`pnpm -F web build` exit 0(仅既有 StatsBoard chunk-size warning)。

- 2026-05-30 `reviewer-multilevel-review`: 实现多级人工审核(初审 first / 复审 second / 终审 final)与审核结果列表(`apps/api` only,无 DB migration,复用 `human_reviews.Stage` 列)。① **stage 派生**:`review.StageForApproveCount(approveCount)` 由「当前 revision(`current_revision_id`)已有的 `approve` 行数」映射 stage/level:0→first/1、1→second/2、2→final/3;新增常量 `review.RequiredHumanReviewLevels=3`。② **升级逻辑(`review.Apply`)**:同一事务内先锁 submission 再 COUNT 当前 revision 的 approve 数,写入带正确 `Stage` 的 `human_reviews` 行 + audit_logs;**中间级 approve**(approveCount<2,即第 1、2 次)只 advance(乐观锁 WHERE status 校验未被抢先终态化 + UPDATE updated_at),submission **停留 `human_reviewing`**,不动 task_items/tasks,audit 事件记 `human_approve_stage`;**终审 approve**(第 3 次)走原 approve→approved 全链路(finish item + `finished_items+1`)。reject/revise 在任意 stage 不变:直接到 `rejected`/`revising`,reason 仍必填(≥5 字符)。revise→labeler 重提产生新 revision→approve 计数随 current revision 归零→stage 自动重置。乐观锁/幂等/状态机校验全部保留;`ApplyResult` 加 `Stage` 字段。③ **stage 暴露**:`ReviewerDetail` 与 `ReviewerQueue` 每条 submission 附 `reviewStage`(first/second/final)/`reviewLevel`(1/2/3)/`requiredLevels`(3),queue 用一条 grouped COUNT 避免 N+1;`POST /submissions/:id/review` 与 `/reviews/batch` 响应回带 `stage`。`BatchReview` 文档化:batch approve 每次只 advance 一级。④ **结果列表**:新增 `GET /reviewer/results`(RequireRoles reviewer/admin),列已定稿(approved/rejected)submission,可见性与 ReviewerQueue 完全一致(admin 全量;reviewer 仅 task_reviewers 授权的 task),返回 id/taskId/itemId/status/finalVerdict/reviewerId(最近一条 human_review 的审核人)/aiScore/updatedAt,按 updatedAt DESC + id 游标翻页(`limit`/`cursor`,emit `next_cursor`/`has_more`)。文件:改 `internal/service/review/review.go`、`internal/handler/reviewer.go`,新增 `internal/handler/reviewer_levels_test.go`,更新 `review_test.go`/`security_test.go`/`integration_test.go` 既有 approve mock 序列(补 approve-count 查询、单 approve 不再直接 approved)。仍需提升:无独立「重置 stage」端点(靠 revise 自然重置);`reviewerId` 取最近一条 human_review(终态决定人),非全链路审核人列表;stage 仅顶层 submission 维度,未做按 stage 过滤 queue。验证:`go build ./...` 通过;`go vet ./internal/...` 无问题;`go test ./internal/handler/... ./internal/service/... ./internal/statemachine/... -count=1 -race` 222 通过;全量 `go test ./...` 281 通过。新增测试覆盖:approve×3 升级(#1/#2 停 human_reviewing、#3→approved)、复审阶段 reject→rejected、`/reviewer/results` 列表+鉴权、queue/detail stage 暴露、`StageForApproveCount` 边界。

- 2026-05-29 `owner-41-task-management-frontend`: 实现「4.1 任务管理 · 任务负责人后台」的**前端**,接通同名后端端点并贴合组织方参考视觉(`~/labelhub-ui-demo` Tasks)。**只动** `apps/web/src/modules/owner/**` + `src/shared/api/client.ts` + `src/styles/lh/tasks.css`,`renderer/`/`modules/labeler/`/`modules/template/` 零改动。① **client 封装**:`client.ts` 扩展 `Task` 类型(richDescription/tags/rewardConfig/distribution/quotaPerUser/deadline/publishedAt),新增类型 `TaskDistribution`/`TaskInfoInput`/`TaskAssigneeView`/`PreviewItem` 等,新增 typed wrappers `createTask`(POST `/tasks`,返回裸 task)、`updateTask`(PUT `/tasks/:id`,解 `{task}`)、`transitionTask`(POST publish/pause/resume/end,解 `{task}`,422 由 ApiError 友好化)、`importItemsFile`(multipart 经 `apiUpload`)、`importItems`(JSON `{items}`)、`batchUpdateItems`、`previewItem`(解 `{item}`)、`listAssignees`/`addAssignees`/`removeAssignee`。② **组件结构**:新增 `TaskManagePanel.tsx`(stat 卡片行 + 任务表格[状态 pill+进度条+分发策略] + 「+新建任务」+ 右抽屉 + 状态感知 发布/暂停/恢复/下线 按钮)、`TaskForm.tsx`(create/edit:标题/简介/富文本说明[textarea→`{html}` JSON]/标签 chips→tags 数组/奖励金额+单位→rewardConfig/截止时间 datetime-local/每人配额/分发策略选择器)、`ImportPanel.tsx`(文件上传 .json/.jsonl/.xlsx→import-file + JSON 粘贴→import + 题目预览 via item-preview + 批量编辑[拉真实 itemId 进可编辑列表]→batch-update)、`AssigneePanel.tsx`(distribution='assigned' 时按 userId 增删指派)、`taskManageHelpers.ts`(纯函数:tags/reward/richDescription 解析、统计卡从真实任务计算、状态/分发标签、可用迁移)。③ **接线**:`Dashboard.tsx` 顶部挂 `TaskManagePanel`,新增 `applyTaskSaved`(合并 saved task 进列表、保持/切换选中,不走 selectTask 全量重置以保护 AI 面板状态)与 `reloadTasksKeepSelection`(导入后 total_items 变化时刷新但保持选中);任务选择仍由既有 `selectTask` 单一来源驱动。④ **改皮 vs 仅逻辑**:任务管理面用 lh 类(`lh-stat`/`tasks-table`/`tasks-drawer`/`taskform__*`)重做成 demo 视觉;既有 AI prompt/golden/dry-run/StatsBoard/ExportPanel 面板**逻辑与 DOM 一字未改**(保护既有 48 测试),仅新增组件引入 lh 皮肤。⑤ **诚实边界**:无 owner「列出全部题目」端点,批量编辑通过 `item-preview` 逐题拉真实 itemId 进列表(不造数据);分发策略 selector 三态齐全,assigned→指派管理、quota→配额输入(在基础信息里),first_come 无额外控件。仍需提升:批量编辑因无 list-items 端点只能逐条加题;富文本是 textarea 存 `{html}`(非所见即所得);截止时间用原生 datetime-local(未上 Semi DatePicker 以简化测试 mock);关联模板切换仍走既有「模板 Designer」链接,未在抽屉内做模板选择。验证:`pnpm -F web exec vitest run src/modules/owner` 全绿(48 既有 + 新增 `TaskForm.test.tsx` 3 条[create 提交序列化/空标题拦截/编辑回填]、`TaskManagePanel.test.tsx` 4 条[卡片+表格渲染/publish 迁移/422 友好 ApiError/抽屉导入文件]);`client.test.ts` 扩 9 条 wrapper 测试(mock fetch 校验 URL/method/body/解包 + 422 INVALID_STATE 友好化);全量 `pnpm -F web exec vitest run` 153/153 通过;`pnpm -F web exec tsc -b` exit 0;`pnpm -F web build` exit 0(仅既有 StatsBoard chunk-size warning);`pnpm -F web lint` clean。

- 2026-05-29 `owner-41-task-management-backend`: 实现「4.1 任务管理 · 任务负责人后台」的完整后端(`apps/api` only,无 DB migration,列已存在)。① **基本信息写**:扩展 `CreateTask` 接受 title/description/richDescription/tags/rewardConfig/distribution/quotaPerUser/deadline;新增 `PUT /tasks/:taskId`(owner/admin,需拥有)做部分字段更新。tags/rewardConfig/richDescription 作为原始 JSON 校验后存进模型的 `*string` json 列;distribution 校验 ∈ {first_come, assigned, quota}(默认 first_come)。② **任务状态机**:`internal/statemachine/task.go` 独立于 submission 状态机,定义 draft/published/paused/ended 与迁移 publish(draft→published,设 PublishedAt=now)/pause(published→paused)/resume(paused→published)/end(published|paused→ended)。新增 `POST /tasks/:taskId/{publish,pause,resume,end}`(owner/admin,需拥有),非法迁移返回 422 INVALID_STATE,publish 要求已绑定模板(TemplateID!=nil)否则 422;迁移用乐观 WHERE status + 同事务写 audit_logs。③ **数据集导入**:`POST /tasks/:taskId/items/import-file`(multipart `file`+可选 `format`),解析 JSON 数组或 `{items:[...]}`、JSONL(逐行 JSON)、XLSX(首行表头映射每行成 payload,经 excelize);复用既有 FirstOrCreate 去重 + total_items 重算逻辑(抽成 `insertImportedItems`),既有 JSON `import` 端点不变。④ **批量编辑**:`POST /tasks/:taskId/items/batch-update` 覆盖指定题目 payload,WHERE 限定 task_id 防越权,批量上限复用 maxImportItems。⑤ **指派**:`GET/POST /tasks/:taskId/assignees` 与 `DELETE /tasks/:taskId/assignees/:userId` 管理 task_assignees(task 级,item_id IS NULL,新增幂等)。⑥ **分发约束(submission.Claim)**:仅作用于领新题路径——quota 下 labeler 在本任务 submission 数 ≥ QuotaPerUser(>0)拒绝(ErrQuotaReached);assigned 下未在 task_assignees 的 labeler 拒绝(ErrNotAssigned);first_come 不变,既有并发/published 检查保留;labeler ClaimItem 把两个新错误映射成 409/403。文件:新增 `statemachine/task.go`、`handler/task_import_file.go`、`handler/task_manage.go` 及各自测试,改 `handler/task.go`/`handler/body_limits.go`/`handler/labeler.go`/`service/submission/claim.go`;excelize 从 indirect 升为直接依赖。仍需提升:导入文件 happy-path 假定全为新行(mock 未覆盖部分重复);assignees 仅 task 级,未做 item 级指派;批量编辑不改 total_items/状态;publish 未校验题目数 >0。验证:`go build ./...` 通过;`go vet ./internal/...` 无问题;`go test ./internal/handler/... ./internal/statemachine/... ./internal/service/submission/...` 全绿(174 通过,含 19 新 handler 测试 + 4 分发 claim 测试 + 任务状态机测试);全量 `go test ./internal/...` 263 通过。

- 2026-05-29 `designer-visiblewhen-customrule-dragplace`: 给 Template Designer 暴露引擎已支持但前端未接的两项 schema 能力,并补拖拽放置。**只动** `apps/web/src/modules/template/Designer.tsx`(+ 集成测试),`renderer/**` 与 labeler 模块零改动。① **属性面板改 3 tab**:`PropertyPanel` 由原扁平堆叠改为「基础 / 校验 / 联动」三页(原生按钮 tablist,沿用既有原生控件风格,不引新 UI 库,保持测试 mock 不变);切换选中字段时用 render-time ref 模式重置回「基础」(避免 effect 内 setState 触发 lint `react-hooks/set-state-in-effect`)。tab 仅对答案型 widget(Input/TextArea/Radio/Tags/RichText/JSONEditor/FileUpload)显示;ShowItem/LLMTrigger/Group/Tabs 仍只展示基础项。② **联动 tab 的 `visibleWhen`**:`VisibleWhenControls` 给「控制字段」下拉(列出其它答案型字段,排除自身)+ `equals` 文本框 或 `notEmpty` 勾选;选中控制字段即默认写 `equals=''`(保证选完立刻是 parser-valid),清空控制字段则 `visibleWhen=undefined` 删除联动。③ **校验 tab 的 `customRule`**:`CustomRuleControls` 给 `expr` textarea + `message` 输入框 + 一行可用变量提示(`value`/`len(value)`/`answer.<字段>`/`and`-`or`-`not`);两者皆空→不写 `customRule`,否则写 `{expr,message}`;另用同一 `expr-eval` Parser 给非阻断的解析提示(解析失败只提示、不挡保存,后端 parser 仍会在 load 时校验)。④ **拖拽放置**:物料按钮加 `draggable`,`dragstart` 往 `dataTransfer` 写 `labelhub/new-widget:<Widget>` 标记;画布 `onDrop` 统一走 `handleCanvasDrop`——以前缀区分:带标记→在落点位置**插入**该类型新字段(落在某字段上=插到它前面,落到画布空白/末尾=追加),无前缀→按原逻辑**重排**已有字段。点击添加(`appendField`)与原有 ↑↓/拖拽重排全部保留。⑤ **序列化(关键)**:`stripDraftField` 是 allowlist,原本会丢 `visibleWhen`/`customRule`,已补进允许字段(并顺带补 `requiredWhen`,此前同样被静默丢弃,属潜在数据丢失);`normalizeFieldSchema` 在 widget 切到非答案型时清除 `visibleWhen`/`customRule`,避免残留。仍需提升:`equals` 目前只支持字符串值(引擎 `equals` 可为任意类型,但 UI 仅文本输入,够覆盖 demo 的 `decision == "reject"` 类场景);拖拽落点是「插到目标字段前」粒度,未做更细的上/下半区判定;Group/Tabs 嵌套子字段仍不支持 visibleWhen/customRule 配置(本次按范围只做顶层字段)。验证:`pnpm -F web exec vitest run src/modules/template` 16/16 通过(14 既有 + 新增「visibleWhen+customRule 配置→保存 schema→`parseTemplateSchema` 往返」「物料拖拽插入到落点」两条);`pnpm -F web exec tsc -b` exit 0;`pnpm -F web build` exit 0;`pnpm -F web lint` exit 0(Designer.tsx 单独 eslint 亦 clean)。

- 2026-05-29 `labeler-43-workbench-feature`: 在 reskin 基础上把「4.3 标注员工作台」完整功能补齐(不破坏既有 claim/draft/submit/autosave/校验/快捷键/`<SchemaRenderer>` 逻辑)。`Plaza.tsx` 现分两视图:① **任务广场**(默认,无活动任务时)—— 搜索框按标题客户端过滤 + 状态下拉过滤 + 任务卡片(进度条),点卡片进入该任务作答页;并列「我的数据」tab。② **作答页**(进入任务后)—— 三栏布局,左栏「题目导航」改由新后端端点 `GET /tasks/:taskId/labeler/items` 驱动,展示全量题目(`#序号` + externalId + 彩色状态点:待标/进行中/草稿/已提交/通过/打回/待修改),头部显示 `已完成/总数 + 进度条 + %`(done=有提交动作的题数);底部工具栏新增 `上一题/下一题`(在导航列表里相对移动)、`跳过`/`领取下一题`(复用既有 `claim`)、`报告题目`(Toast 占位,后端无端点);中栏 head/meta/打回横幅 + **原封不动的 `<SchemaRenderer>`(props 一字未改)** + autosave 指示 + 保存草稿/提交按钮;右栏「我的贡献(本任务)」从端点 counts 派生真实数值(已提交/通过/打回,仅展示有值项)+ 「本题历史」(`bundle.auditLogs`)+ 快捷键。**我的数据**:从 `GET /me/submissions` 聚合 已提交(submitted+ai_reviewing+human_reviewing)/通过/打回/待修改 计数 + 可点击列表(点「打开」进入对应题目作答页)。导航规则:选中 `mine`/有 `submissionId` 的题 → `apiGet('/tasks/:taskId/items/:itemId')`(新增 `openByItem`,镜像 `openSubmission`);选 available/跳过 → `claim`。端点失败降级:Toast 提示 + 退回仅领取导航,不崩。新增文件:`ItemNav.tsx`/`TaskPlaza.tsx`/`MyData.tsx`/`plaza.css`,`client.ts` 加 `LabelerTaskItem`/`LabelerTaskItems` 类型。**既有 `claim`/`saveDraft`/`submit`/autosave effect(3s+seq guard)/Ctrl-Cmd+Enter effect/校验/`<SchemaRenderer>` props 全部保留未改。** 仍需提升:`报告题目` 是占位(后端无上报端点);右栏贡献来自任务级 counts、我的数据来自全局 submissions(两处口径不同,符合预期);上一题/下一题在端点失败(itemNav=null)时退化为领取下一题。验证:`pnpm -F web exec vitest run src/modules/labeler` 7/7 通过(保留全部既有行为断言 + 新增「我的数据聚合」「题目导航进度」两条);`pnpm -F web exec tsc -b` exit 0;`pnpm -F web build` 通过(仅既有 StatsBoard chunk-size warning);全量 `pnpm -F web` 测试 127/127 通过。

- 2026-05-29 `labeler-form-widgets-reskin`: 把 Labeler 答题表单的 4 个 renderer widget(`renderer/widgets/`)改皮成组织方参考 demo(`~/labelhub-ui-demo` Workbench)的外观,只动渲染 markup/className/CSS,行为零改动。① `Radio.tsx`:`<select>`/原生 radio 行 → 一排圆角分段按钮(选中填充蓝 `--lh-primary`);保留原生 `<input type=radio>`(视觉隐藏)以维持 `role=radiogroup`/`aria-label`/`getByLabelText`/`toBeChecked` 可访问性与 value/onChange/readOnly 语义。② `Tags.tsx`:checkbox → 多选 pill chips(选中=软蓝填充 `--lh-primary-soft`),同样保留视觉隐藏的原生 checkbox,value 仍为 `string[]`,toggle/onChange/readOnly 不变。③ `LLMTrigger.tsx`:外壳改成紫色虚线卡片(`--lh-purple` dashed + `--lh-purple-soft` 底),含 ✦ 标题行、已有结果时显示「重新生成」(复用同一 `run()`)、结果预览与原「运行 AI 预审」紫色主按钮;**fetch/`target_field`/`onChange`/readOnly 逻辑一字未改**,仅重排容器/标记。④ `Input.tsx`:`field.maxLength` 存在时在输入框右下角加 `current / maxLength` 字数计数(如 `17 / 35`),value/onChange/maxLength 行为不变。沿用 widgets 既有 inline `CSSProperties` 风格(无 widgets.css),配色用 `lh/tokens.css` 的 `--lh-*`(蓝 `#165dff`/紫 `#722ed1`,已由 `main.tsx` 的 `lh/globals.css` 全局提供)。**未改任何 widget 的 props 接口/value 处理/onChange 契约/校验/LLMTrigger fetch 与 target_field 逻辑/readOnly 支持**——Reviewer 以 readOnly 复用同组 widget,四者在 readOnly 下仍静态、非交互、不触发 onChange。仍需提升:LLMTrigger 紫卡正文未捏造 demo 的固定建议文案(无真实值时不渲染);分段/pill 选中态配色沿用 demo,未做暗色态。验证:`pnpm -F web exec vitest run src/renderer` 28/28 通过(测试零改动,全部行为断言保留——radiogroup/toBeChecked/getByLabelText/LLM run+target_field 均未受影响);`pnpm -F web exec tsc -b` 通过(exit 0)。

- 2026-05-29 `labeler-plaza-workbench-reskin`: 把 Labeler 工作台 `modules/labeler/Plaza.tsx` 的 JSX 改皮成组织方参考 demo 的三栏工作台布局(`wb-shell` / `wb-side` / `wb-main` / `wb-right`),import `styles/lh/workbench.css`(其依赖的 `--lh-*` token 已由 `main.tsx` 引入的 `lh/globals.css` 全局提供)。映射真实数据:左栏「题目导航」由现有 `tasks` + revising `mySubmissions` 驱动,每项调用原有 `claim`/`openSubmission`,状态用现有 `StatusBadge`,当前 bundle 高亮;中栏标题取 `schema.title`、meta 拼 `任务 ID / 题目 ID / 模板版本`,打回横幅仅在 `latestHumanReview.reason` 存在时显示,中央仍是原封不动的 `<SchemaRenderer>`(props 一字未改),footer 保留原「保存草稿/提交审核」按钮 + autosave 文本;右栏「本题历史」时间线由 `bundle.auditLogs` 真实生成(actor/event 中文映射 + 月日时分格式化),并保留 Ctrl/Cmd+Enter 快捷键提示。**未捏造** demo 的固定 NAV/CATEGORIES/KEYWORDS/AI 建议数组与 62/54/5 贡献数字(无真实值即省略)。**只动返回的 JSX + className + 一个 CSS import**,所有 state/hooks/handlers/effects/refs/API 调用/校验/autosave 逻辑零改动;移除了改皮后不再使用的 inline-style 常量与 `ProgressBar`、`CSSProperties` import。仍需提升:left 导航与全局 AppLayout 侧栏并存(两者职责不同,符合预期);贡献统计(已提交/通过/打回)因当前 state 无真实聚合值而整体省略,后续若后端补字段可补回。验证:`pnpm -F web exec vitest run src/modules/labeler/Plaza.test.tsx` 5/5 通过(测试零改动,行为断言全保留);`pnpm -F web exec tsc -b` 通过;`pnpm -F web build` 通过(仅既有 StatsBoard chunk-size warning);`pnpm -F web exec eslint src/modules/labeler/Plaza.tsx` 通过。
- 2026-05-29 `deploy-wire-doubao`: 把生产 `deploy/.env` 的 LLM 从 mock 切到真豆包(`LLM_PROVIDER=doubao`,`LLM_BASE_URL=https://ark.cn-beijing.volces.com/api/v3`,`LLM_MODEL=ep-20260514105718-jthdm`)。新值从本地 `.env` 解析后经 stdin 管道同步(命令文本只含变量引用、明文 key 不进 transcript/不进 git),服务器侧只替换 5 个 LLM 字段并 `up -d api worker` 重启(不重建镜像)。验证:服务器直连豆包 `chat/completions` 返回 200/2.4s(pong),worker 干净启动无 LLM 配置错误。至此 demo(http://43.155.210.70)AI 预审为真实链路。
- 2026-05-29 `deploy-prod-tencent-seoul`: 首次把整套栈部署到自有云服务器(腾讯云轻量·首尔节点,免备案,2C4G + 4G swap),公网可访问 **http://43.155.210.70**。流程:加 swap → rsync 本地最新代码(含未推送的手机端/角色修复)→ 在服务器生成生产 `deploy/.env`(`openssl` 随机密钥 + `:80` IP 直连 + `API_CORS_ORIGINS=http://43.155.210.70` + `LLM_PROVIDER=mock`,asynqmon basic-auth hash 用 `$$` 转义)→ `compose up -d --build`。修复一个生产部署**阻断 bug**:`deploy/asynqmon/Dockerfile` 的 `go install github.com/hibiken/asynqmon@latest` 因上游 v0.7.2 根目录已非 main 包构建失败 → 整个 `up` 中止;改为 compose 直接用官方预构建镜像 `hibiken/asynqmon:latest`(distroless 无 `sh`,弃用 `sh -c`,Redis 密码在 compose 解析期插入命令参数)。api 镜像不含 seed 二进制,用一次性 `golang:1.26-alpine` 容器接 `deploy_default` 网络跑 `cmd/seed`(`SEED_ALLOW_IN_PROD=true` 放行 release)。回填 `submission/DEMO_ENV.md` 真实地址,密码从误写的 `pass` 改正为 seed 实际的 `123456`、任务名改正为 `qa_quality`/`preference_compare`。仍需提升:① 现为 HTTP+IP,挂域名可自动 HTTPS;② 仍是 mock provider,待切真豆包;③ `deploy/asynqmon/Dockerfile` 已弃用(可删);④ README/DEMO_SCRIPT 仍写 `pass`,待统一改 `123456`。验证:`/health`、`/api/v1/` 外网 200;owner1/labeler1/reviewer1 用 `123456` 外网登录均 200 返回 JWT;7 个容器 Up,mysql/redis healthy。
- 2026-05-29 `s7-review-p1-batch`: 收口 review 列出的 P1 体验/加固项(P0 之后)。① Seed 硬化:`cmd/seed/main.go` 不再把 demo 密码值打进日志,且 `GIN_MODE=release` 时默认拒绝运行(`SEED_ALLOW_IN_PROD=true` 可覆盖),避免误把 demo 弱口令灌进生产库(本地 `make seed` 不受影响,本地 `.env` 未设 release)。② `index.html` `<title>` 从默认占位 `web` 改为 `LabelHub · 数据标注平台`。③ 批量导入 DoS 加固:`handler/task.go` `ImportItems` 改用 `bindLimitedJSON`(新增 `maxImportItemsBytes` 8MB)并加单批 `maxImportItems` 10000 条上限。④ `owner/Dashboard.tsx` golden sample dry-run:queued 响应缺 `dryRunId` 时直接置 failed,不再用无效 id 空转轮询。⑤ a11y:`tokens.css` focus-visible 去掉零特异性的 `:where()`、改普通选择器列表并显式覆盖 `.semi-button/-input/-select/-input-textarea`,保证键盘焦点环在 Semi 组件上也可见;新增 `@media (prefers-reduced-motion: reduce)` 停掉 skeleton/top-progress 无限动画并把过渡降到近瞬时。⑥ 响应式:新增 `styles/layout.css`(`.lh-shell-2col/3col`,≤1024px 收成单列)并在 `main.tsx` 引入,Reviewer Queue / Labeler Plaza / Owner Dashboard 三个多列工作台外壳由内联 grid 改为 className——桌面列宽不变,仅窄屏收敛,消除横向溢出(此前仅 Designer 响应式)。⑦ CSV:新增 `TestEncodeCSV_NeutralizesNewlinePrefixedFormula`(`\n=HYPERLINK(...)` 真实 payload 回归)。说明:Reviewer 上传 IDOR 的直接回归测试已在 `s7-review-p0-batch` 落地(`TestUploadReviewerNotAssignedForbidden`),本批未重复;已绑定→成功的 happy-path 因需磁盘写+INSERT mock 暂仍未覆盖。验证:`gofmt` 干净;api 模块 `go build`(含 cmd/seed)通过;`go test ./apps/api/internal/handler/... ./pkg/exporter/...` 通过;`pnpm -F web exec tsc -b` 通过;Plaza/Queue/ExportPanel 测试通过、`Dashboard.test.tsx` 单独 36 通过(批量里偶发争用超时,隔离稳定);`pnpm -F web lint` 通过。
- 2026-05-29 `s7-review-p0-batch`: Opus 4.8 多角度全栈 review 后修复 6 个 P0,并补对抗式验证与回归测试。① Reviewer 上传越权(IDOR-write):`handler/upload.go` `canUploadToTask` reviewer 分支改为先过 `canReviewTask`(`task_reviewers` 绑定校验)再查 human_reviewing 提交——未指派的 reviewer 不能再往任意有人工审核提交的 task 写文件。② 登录限流 XFF 绕过:`cmd/server/main.go` 在 `gin.New()` 后加 `SetTrustedProxies(trustedProxies())`(默认只信回环+私有网段,`TRUSTED_PROXIES` env 可覆盖,非法值 fail-fast),公网客户端伪造 `X-Forwarded-For` 不再绕过按 IP 限流。③ CSV 公式注入:`pkg/exporter/encode_csv.go` `csvSafeCell` switch 补 `\n` 触发字符(经核实 XLSX 走 excelize inlineStr、Excel 不执行公式,无同类风险)。④ 导出下载 URL 未校验:`owner/ExportPanel.tsx` `download` 加 `isSafeURL` 协议白名单校验 + `window.open(..., 'noopener,noreferrer')`。⑤ Reviewer 队列详情竞态:`reviewer/Queue.tsx` `openQueueItem` 加请求序号守卫,并用独立 `detailLoadSeq` 与规则面板 `ruleLoadSeq` 解耦,避免开关「规则配置」误失效正在加载的详情。⑥ Editorial 强调色回归:`styles/tokens.css` `--color-accent/-hover/-soft/-rail` 从蓝系(`#1f5d99…`)改回暖红(`#b8442e/#97331f/#f5e4dd/#cf9483`),`StyleGuide.tsx` 页脚同步。新增/更新测试:`upload_test.go` 加 `TestUploadReviewerNotAssignedForbidden`(未绑定 reviewer→403 IDOR 守卫)、`encode_test.go` `TestCSVSafeCell` 补 `\n`/`\r` 用例、`ExportPanel.test.tsx` 更新为 3 参 `window.open` 断言并新增 `isSafeURL` 拒绝 `javascript:` 负向测试。仍需提升:reviewer 上传 happy-path(已指派+human_reviewing→成功)仍无测试(需磁盘写+INSERT,本次跳过);新 accent `#b8442e` 与 `--color-danger #b33a32` 色相接近(ΔE≈6.4),品牌/错误区分偏弱,可按需再拉开;`--color-port/--color-info-bg/--status-submitted-*` 仍为蓝系(本次按范围未动);review 列出的 P1(`ImportItems` body 上限、seed 在 release 环境拒跑、Dashboard queued `dryRunId` guard、`<title>` 占位、focus-visible `:where` 特异性、reduced-motion、非 Designer 页响应式、validator trim/maxLength、JSONEditor `{}` required 语义)按用户 triage 留待后续。验证:`go test ./apps/api/internal/handler/... ./apps/api/internal/policy/... ./pkg/exporter/...` 通过;api 模块 `go build`/`go vet` 通过;`pnpm -F web exec tsc -b` 通过;`ExportPanel.test.tsx` 7 通过、`Queue.test.tsx` 9 通过;6 项修复经独立对抗式 verifier 复核均 closesIssue=yes 且无回归;`.env` 经核实未入 git(`.gitignore`+`.dockerignore` 覆盖),不需轮换。
- 2026-05-29 `s7-role-login-rbac`: 补齐 LabelHub 三角色登录与模块隔离。Seed 统一密码改为 `123456`,拆分 owner/labeler/reviewer 三个独立 demo 账号;前端登录页改为角色快捷登录;owner 不再进入 reviewer 视图,reviewer 不再继承 owner/labeler 越权,labeler item 详情也收紧为 labeler/reviewer/admin;AI Agent 继续由后端 worker/system 账户承载,审计日志仍可追溯。验证:`go test ./apps/api/... ./apps/ai-worker/... ./pkg/exporter ./pkg/llmreview -count=1` 通过;`pnpm -F web test -- --pool threads App.test.tsx modules/auth/Login.test.tsx modules/reviewer/Queue.test.tsx` 通过;`pnpm -F web lint` 通过;`pnpm -F web build` 通过;`git diff --check` 通过。
- 2026-05-29 `s7-demo-accessibility-fix`: 收尾 reviewer demo 和导航可见性问题。Reviewer demo 详情不再静态:切换不同 demo submission 时,左右比较面板、JSON 预览、AI Prompt 和时间线都会跟着选中项更新;AppLayout 侧边栏去掉 owner → reviewer 的泄漏入口;`index.html` lang 改为 `zh-CN`。验证:`pnpm -F web test -- --pool threads App.test.tsx modules/auth/Login.test.tsx shared/layout/AppLayout.test.tsx modules/reviewer/Queue.test.tsx` 通过;`pnpm -F web lint` 通过;`pnpm -F web build` 通过;`git diff --check` 通过。
- 2026-05-28 `s7-review-cleanup-batch-2`: 第二批 review 项收口。① Dashboard.tsx 切到 preference_compare 时描述还显示硬编码"官方 qa_quality 主线任务"(review #M5,smoke 真实重现),改用 `selected.description ?? '配置标注模板与 AI 预审参数。'`。② Reviewer 工作台静态 demo 占位清理:`!showingDemo` 时隐藏假 model pill `Agent v2.3 · doubao-pro-32k`、假 filter tab (128/47/9)、假 SLA card (38/s · T-2041)、假 MetricGrid (214/87%/47/02:14:00);Timeline 空 audit logs 时换 EmptyState 而不是 fallback 到"李雷/王芳/AI Agent 第 1 轮提交"6 行假事件;breadcrumb `!showingDemo` 用 `detail?.task.title` 替代硬编码"商品标题清洗 v3"。Demo 模式保留全部静态占位(因为 demo 数据本来就是 placeholder)。③ M7 Caddy 加 CSP + Referrer-Policy + Permissions-Policy + 去 Server header (markdown 走 JSX,CSP 严格 self only)。④ M8 Redis 加 `--requirepass`,asynqmon/api/worker 配套加 `REDIS_PASSWORD` env,prod compose 用 `${REDIS_PASSWORD:?}` 强制,`deploy/.env.example` 补一行 + `openssl rand -hex 32` 说明。M3 review.Apply probe SELECT 风险评估后跳过(改锁顺序需全代码 audit 避免死锁,probe 损耗 < 1ms,demo-acceptable)。本地验证:`go test ./apps/api/... ./apps/ai-worker/... ./pkg/exporter ./pkg/llmreview` 306 通过;`pnpm -F web build/lint/test` 117 通过;`docker compose --env-file deploy/.env.example -f deploy/docker-compose.prod.yml config` 通过。
- 2026-05-28 `s7-reviewer-detail-p0-fix`: S7 浏览器 smoke 触发的 P0 修复。Reviewer detail 点击真实 submission 时 `RealAIDiagnostics` + `Timeline` 两个组件抛 `TypeError: Cannot use 'in' operator to search for 'Valid' in draft`,ErrorBoundary 接管,任何真实 submission 都进不去 detail 页(demo 日 reviewer 流程彻底废)。Root cause:`apps/web/src/shared/api/client.ts:232` `AuditLog.fromState` type 声明为 `{ String?: string, Valid?: boolean } | null`(sql.NullString 形状),但后端实际 JSON 序列化输出纯字符串 `"draft"`/`"submitted"` 等;`Queue.tsx:999 auditStateText` 用 `'Valid' in log.fromState` 命中字符串就 TypeError。修复:type 改为 `string | null`,`auditStateText` 简化为 `log.fromState ?? ''`,`Queue.test.tsx:147` mock 也改为字符串(暴露 review #M2 提到的"mock shape 不匹配生产"反模式)。浏览器复跑 reviewer→详情→通过完整链路通过,DB 验证 submission 5 `status=approved/human_verdict=approve/approved_at=...`。
- 2026-05-28 `s7-preference-compare-seed`: S7 答辩冲刺第一块落地: `make seed` 现在除官方 `qa_quality` 外,还会创建第二官方任务 `preference_compare`(12 条偏好对比题)。新增 `tools/seed/templates/preference_compare_review.json`,模板覆盖 Group + Tabs + ShowItem / Radio / Tags / Input / TextArea / RichText / JSONEditor / FileUpload / LLMTrigger,可直接展示 prompt、response_a、response_b 并采集 A/B/tie 偏好结论、优势程度、安全风险、判断维度和理由。Seed 逻辑抽成 `seedOfficialTask`,保留 rerun 不回滚用户后续模板版本的行为。
- 2026-05-28 `s6-browser-smoke`: S6 Day5 浏览器 smoke 完成(本地起整套栈,因 :8080 被无关 FastAPI 占用,API 跑 :8090、web 代理指过去;用 chrome-devtools 以 owner1 登录核验 login/owner/designer/reviewer/style-guide,跑完已杀进程不留僵尸)。**确认 Designer 响应式真实生效**:1920 三栏并排、1280 物料+画布两栏且属性面板下移占整行——这是 jsdom 测不了的核心点。StatusBadge/Editorial token 一致,无 console error(仅既有 React Router v7 future-flag warning),无文字重叠。smoke 中发现并修复一处不一致:`/style-guide` 仍是旧 Schematic/Inter/#0f62fe 标识 + 自己硬编码的状态标签,现改为 Editorial(色板直接读 CSS 变量、改用真实 `StatusBadge` 全 7 态、标题/footer 更新),`docs/S6_ACCEPTANCE.md` 同步记录。**S6 至此全部完成。**
- 2026-05-28 `s6-acceptance`: 新增 `docs/S6_ACCEPTANCE.md`,记录 S6 Day1–4 全部完成(Editorial tokens/共享状态原语、高密度状态面、Runtime Tabs 真实交互、Designer 响应式+嵌套画布、友好错误文案、a11y、Labeler 快捷键)、刻意砍掉/跳过项(Reviewer 单键快捷键、dark mode、移动端、各类库迁移),以及唯一待办——Day5 浏览器 smoke 需本地起 `make web` 后在 1280/1920/窄屏核验(jsdom 测不了 CSS 断点)。docs-only。
- 2026-05-28 `s6-a11y-shortcut`: S6 Day4 第二段(Task 4.2 a11y + 可选快捷键),Day4 完成。审计确认 Designer 嵌套控件(`NestedFieldsEditor`/`GroupControls`/`TabsControls` 的 name/widget/label/required/options input/select)已全部带 `aria-label`,全局 `:focus-visible` ring 已在 Day1 落地,a11y 必做项无需再改。新增 Labeler `Ctrl/Cmd+Enter` 提交快捷键(ref 持有最新 `submit` + 注册一次 `keydown`,内部已对无 bundle/校验失败兜底;提交按钮加 `title` 提示),补 Plaza 测试。Reviewer 单键 A/R/B 触发审核动作风险高(易误触不可逆操作),计划标"可选"——本次刻意跳过。
- 2026-05-28 `s6-error-copy`: S6 Day4 第一段(Task 4.1 错误文案归一化)。`shared/api/client.ts` 新增 `ApiError`(继承 `Error`,带 `code`/`requestId`,向后兼容现有 `err instanceof Error` 判断),`request`/`apiUpload` 改抛 `ApiError`;对会卡住标注/审核流程的两个码给可执行中文提示——`INVALID_STATE`("状态已被改动,请刷新后再操作")、`LLM_PROVIDER_ERROR`("AI 暂不可用,可重试或转人工"),其余码仍用后端 message,不覆盖既有具体说明。友好文案经任何展示 `err.message` 的地方自动冒出,无需逐模块改。新增 `client.test.ts` 覆盖映射/兜底;过程中抓到并修掉自己引入的 bug——`??` 不兜空串导致后端空 message 时报空错误,改回 `||`。
- 2026-05-28 `s5-review-fixes`: S5 三方 review(Go / TypeScript / 安全)发现的部署模板问题修复。Critical:生产 `deploy/Caddyfile` 把 asynqmon 管理台 `/asynqmon/*` 无鉴权直接反代到公网(dev compose 本来有 nginx basic auth,prod 漏了),改为 Caddy `basic_auth`,凭证 `ASYNQMON_USER` / `ASYNQMON_PASSWORD_HASH` 用 `${VAR:?}` 强制——缺了 compose 直接拒起,杜绝无鉴权暴露。High:`docs/LabelHub.postman_collection.json` 硬编码 `owner1`/`pass`(repo 已公开),改为 `{{ownerUsername}}`/`{{ownerPassword}}` 变量,密码默认空。`docs/DEPLOY.md` 补 asynqmon 鉴权与 `LLM_API_KEY` 在真实 provider 下必填的说明,`.env.example` 补 TLS 域名提示。Go/TS reviewer 确认 S5 生产逻辑(exporter 瞬时/终态重试、stats 仅算当前 revision、StatsBoard request-seq guard、ErrorBoundary)均正确,其余为测试置信度缺口(见下方"待跟进")。
- 2026-05-28 `s6-designer-responsive`: S6 Day3 第二段完成:新增 `apps/web/src/modules/template/Designer.css`(原来 import 了但文件缺失,会直接挂 build),把 Designer 三栏布局从固定 `220px / 1fr / 320px` inline grid 改成响应式——1920 三栏(物料 / 画布 / 属性),≤1599 两栏(物料 + 画布,属性面板下移占满整行),≤768 单列堆叠;修正 Codex 贴错的类名(`template-designer-properties` 本来贴在左侧物料栏上,改为 `template-designer-palette`,并给画布 `template-designer-canvas`、右侧属性面板 `template-designer-property` 补正确类名);Group/Tabs 画布预览改为 `NestedCanvasPreview`/`NestedCanvasRow`,把子字段以 widget/name/label 迷你行展示(Tabs 按 tab 分组),并补集成测试断言画布上嵌套预览确实渲染 Group 子字段和 Tabs 各页子字段。
- 2026-05-28 `s6-runtime-tabs`: S6 Day3 第一段完成: `SchemaRenderer` 的 Tabs 从“所有 tab panel 堆叠显示”改成真实 `tablist`/`tabpanel` 交互,默认显示第一 tab,点击切换后只渲染 active tab 字段,同时保持 flat answer object 中已填写答案不丢;补 `SchemaRenderer` 测试验证 tab 切换、隐藏字段、切回后 radio 选择保持;Designer round-trip 测试跟随新 Tabs 行为先切到 Tab 2 再断言子字段。
- 2026-05-28 `s6-dense-status-surfaces`: S6 Day2 第一段完成:Owner Dashboard、ExportPanel、Labeler Plaza、Reviewer Queue 开始复用共享 `StatusBadge`/`EmptyState`/`LoadingBlock`,导出历史、任务列表、AI review 开关、golden sample、dry-run history、标注空态和审核空态不再直接裸显示 raw status/loading 文本;相关测试断言更新为新中文状态/可访问 loading 状态。
- 2026-05-28 `s6-state-primitives`: S6 Day1 底座完成: `tokens.css` 从 Schematic/Inter/default-blue 收敛为 Editorial console tokens,移除 Google font 依赖,加入稳定 7 态 status 色、Semi 常用控件 CSS override、全局 focus-visible、skeleton/top-progress keyframes;新增共享 `StatusBadge`/`statusLabel`、`EmptyState`、`LoadingBlock`、`TopProgress` 和 `StatePrimitives` 测试,为后续 Owner/Labeler/Reviewer 页面统一状态、空态、加载态做准备。
- 2026-05-28 `s6-plan`: 新增 `docs/PLAN-S6-IMPL.md`,把 Sprint 6 拆成 Editorial token/shared state components、Owner/Labeler/Reviewer 高密度界面、Designer 响应式布局、Tabs/Group 真实交互、友好错误/A11y、Browser smoke 和 `docs/S6_ACCEPTANCE.md` 验收文档。计划明确将 dark mode、Framer Motion、完整移动端、TanStack Query/Formily/dnd-kit 迁移列为可砍项。
- 2026-05-28 `s5-deploy-docs`: S5 Day5 收尾完成:新增生产部署模板 `deploy/docker-compose.prod.yml`,覆盖 api/worker/web/mysql/redis/asynqmon/caddy;新增 api/worker 共用多阶段 Dockerfile、web 静态 Caddy 镜像和 SPA fallback;生产 compose 通过 env 显式注入密钥/LLM/JWT/导出配置,api/worker 共享绝对 `EXPORT_DIR` volume,api 上传目录单独持久化;新增 `deploy/Caddyfile`、`deploy/.env.example`、`.dockerignore`;`make dev` 现在可一键起基础设施、安装依赖、seed 并提示分别启动 api/worker/web;新增 `docs/ARCHITECTURE.md`、`docs/DEPLOY.md`、`docs/S5_ACCEPTANCE.md`。
- 2026-05-28 `s5-openapi-error-boundary`: S5 Day4/Day5 第一段完成:按计划 fallback 手写主流程 `docs/openapi.yaml`(auth/tasks/templates/labeler/reviewer/exports/stats),新增 `pnpm -F web gen:api` 用 `openapi-typescript` 生成 `apps/web/src/shared/api/schema.d.ts`;新增 `docs/LabelHub.postman_collection.json` 覆盖登录、领题、提交、审核、导出、stats 主流程;前端新增全站 `ErrorBoundary`,在 `main.tsx` 包住 `<App/>`,并补抛错 fallback/reset 测试。
- 2026-05-28 `s5-frontend-strict-roundtrip`: S5 Day3 前端质量补强完成:开启 `apps/web/tsconfig.app.json` 的 `strict:true` 且 `pnpm -F web build` 直接通过;Renderer 新增 ShowItem text/video/json 模式测试与 LLMTrigger 写回 `target_field` 测试;Designer 集成测试补保存 payload → `parseTemplateSchema` → `SchemaRenderer` 的 round-trip 断言,覆盖 Group/Tabs 子字段、导出字段和 Renderer 实际渲染一致性。
- 2026-05-28 `s5-integration-main-flow`: S5 Day2 新增 build-tag 隔离的 testcontainers 集成测试 `apps/api/internal/integration/main_flow_integration_test.go`:真 MySQL 8.0.36 + Redis 7 容器,真实迁移 SQL,seed owner/labeler/reviewer/task/prompt/item,覆盖 submit→ai_review outbox→Redis publish→AI 结果入库→review revise→labeler resubmit→AI pass→review approve→JSONL export 落盘的主链路。CI 新增独立 `integration` job 跑 `go test -tags=integration ./apps/api/internal/integration -count=1`,普通单测仍不跑 Docker。
- 2026-05-28 `s5-quality-day1`: 按 `docs/PLAN-S5-IMPL.md` 开始 S5 工程质量 sprint:新增并纳入 S5 执行计划;CI Go workspace 补跑 `./pkg/exporter`;状态机测试改为覆盖 `Transitions()` 暴露的全部合法边与每个状态的非法事件,`internal/statemachine` 覆盖率到 100%;`review.Apply` 补 approve/reject/revise/非法状态/并发写 RowsAffected=0 的事务测试,覆盖率从 13.0% 提升到 70.1%;outbox publisher 补 stale processing reset、claim lost update、enqueue 失败重试/死信、TaskID 冲突幂等成功测试,覆盖率从 29.1% 提升到 45.5%;AI worker 补 `handleAIReview` verdict→状态 table-driven 覆盖(pass 自动通过/pass 转人工/reject 转人工/uncertain 转人工)和熔断 open 直接 failover 到人工路径;修复 S4 review 遗留两处质量问题:导出行加载 DB 错误不再被写成 terminal failed,保持 asynq 可重试;StatsBoard 维度均分只统计 `ai_reviews.revision_id = submissions.current_revision_id`,避免旧 revision AI 结果污染看板。前端 StatsBoard retry 改 request sequence guard,切任务后晚到 retry 响应不再覆盖当前任务。
- 2026-05-28 `s4-review-fixes`: 修复 S4 code review(go/typescript/security 三审 + 人工核验)发现的 5 个 HIGH 与 2 个 MEDIUM:① api/worker 启动强制 `EXPORT_DIR` 绝对路径(相对路径在两进程不同 CWD 下会让签名下载静默 403),`.env.example` 改绝对示例;② `pkg/exporter` 新增 `ErrTerminal` 区分永久失败(编码/落盘,已写 `failed`)与瞬时失败(DB),worker 据此 `asynq.SkipRetry` 或重试,并对不可解析 payload 直接 SkipRetry;③ CSV 导出对 `= + - @ \t \r` 开头单元格加前导单引号,防 Excel/LibreOffice 公式注入;④ ExportPanel 历史加载 `catch` 补 stale guard,切任务后晚到失败不再误弹 Toast;⑤ StatsBoard 增加错误态 + 重试按钮,fetch 失败不再永久转圈;⑥ VChart 改 `React.lazy`,并在 `vite.config` 让 `@visactor` 脱离 eager vendor,首屏 eager vendor 从 2.2MB(gzip 616KB)降到 412KB(gzip 125KB),VChart 进按需加载的 StatsBoard chunk。顺带把 AI 饼图负值用 `Math.max(0, …)` 兜底。
- 2026-05-27 `s3-hardening-cleanup`: 收尾 S3 hardening 未提交修复:状态机补 `ai_auto_approved` transition 并纳入全表覆盖;`GET /uploads/:id` 复用 task 上传权限策略下载非 deleted 文件;outbox publisher 在事务内只取 pending event、事务外 enqueue,并用 deterministic Asynq `TaskID(outbox:<id>)` 防重复投递;AI worker 5xx 熔断默认调整为连续 20 次/5 分钟;API 主进程新增 hourly orphan temp upload cleaner,24h 以上未 attached 的 temp 记录会先条件标 deleted 再删除磁盘文件。
- 2026-05-27 `s3-review-finding-fixes`: 修复提交 diff review 的三处回归/边界问题:所有已有 submission 的答题 bundle 都固定按 `submission.template_version` 读取历史模板,避免待审/详情状态在 owner 改模板后误用当前 schema;Reviewer 规则面板改回只读查看 + 跳 Owner 编辑,移除 reviewer 侧直接启用 AI prompt 的 mutating route;ai-worker 的 golden dry-run 在 provider circuit open 时会先 claim run 再写 failed,避免 owner 轮询长期停在 queued。
- 2026-05-27 `s3-durable-dry-run-rule-ops`: 收尾 S3 demo 稳定性缺口:单条 golden sample dry-run 从 API 进程内 goroutine 升级为 durable outbox/asynq 链路,API 同事务写 `ai_dry_runs` + `outbox_events(ai:dry-run)`,ai-worker 消费后读取 prompt/sample snapshot、调用 provider、写 succeeded/failed 结果;Reviewer 规则配置可查看历史/当前 prompt 并跳转 Owner 编辑;Owner dry-run history API 返回 guard snapshot,Dashboard 展示 quota/circuit 状态、匹配率和均分。
- 2026-05-27 `s0-s3-plan-gap-polish`: 补齐第一批 S0-S3 计划缺口:新增 GitHub Actions CI,覆盖 Go workspace、web test/lint/build;deploy compose 增加 `ops` profile 下的 asynqmon + nginx basic auth;`/style-guide` 补齐 Button/Card/Form/Table/Modal/Tag 六类核心组件展示;Labeler Plaza 增加 3s debounce 自动保存草稿并带 request identity guard;schema parser/validator 和后端模板校验支持 `regex` 与 `requiredWhen`;AI worker 增加连续 provider HTTP 5xx 熔断,成功响应自动重置。
- 2026-05-26 `s3-demo-productization-closure`: 按获奖 demo 可见度补齐 S3 产品闭环:Owner Dashboard 可直接编辑并保存 `baseline_description`;AI Prompt dimensions 从 JSON 主路径升级为可增删改表单,threshold 增加 slider+number 控制;单条 golden sample dry-run 改为创建 queued run 后后台执行并由前端轮询 pending/running/succeeded/failed;AI worker 支持 `pass + human_review_enabled=false` 自动 approved 并写 `ai_auto_approved` audit;Labeler Plaza 增加“待修改”入口和上一轮打回意见;Reviewer Queue 增加多选批量 approve/revise 与上一轮意见展示,后端新增 `/reviews/batch`。
- 2026-05-26 `s3-reviewer-rule-config-entry`: Reviewer 的“规则配置”从静态提示升级为真实任务规则入口:新增 reviewer/owner/admin `GET /reviewer/tasks/:taskId/ai-prompts`,按 `task_reviewers`/owner/admin 边界列出该 task 的 AI Prompt versions、active prompt 和 AI review 开关;Reviewer Queue 右侧可打开规则面板、选择历史/当前规则版本查看 prompt/dimensions/threshold/model,并通过 `/owner?taskId=...&aiPromptId=...#ai-prompts` 跳转 Owner 编辑对应规则版本;Owner Dashboard 支持该深链初始选中 task 和 prompt version;前端补规则加载 stale guard,避免切换提交后晚到规则响应污染当前面板。
- 2026-05-26 `s3-reviewer-ai-detail-wiring`: Reviewer Detail 接入真实 AI 预审详情:后端 `/reviewer/submissions/:submissionId` 返回当前 submission/revision 的 latest `ai_reviews`、对应 prompt version 元数据和 submission audit logs;新增 `POST /reviewer/submissions/:submissionId/ai-review/retry`,仅允许 failed/dead AI review 重置为 pending 并重新写入 outbox;前端 Reviewer Queue 优先渲染真实 AI verdict/score/dimensions/reason/tokens/latency、Prompt 模板和处理日志,并把失败重跑按钮接到真实 API。
- 2026-05-26 `s2-designer-nested-visual-editing`: S2 Designer 把 Group/Tabs 子字段属性栏从 JSON textarea 升级为直接编辑控件:可改子字段 name/label/widget/required/options,可添加/删除/上下移动子字段,可增删 Tabs tab 并改 tab label;保存仍走原有递归 schema/export_fields 路径。修复嵌套编辑行用正在编辑的 child name 做 React key 导致输入中断、重复 name 校验误触发的问题。
- 2026-05-25 `s2-s3-structure-and-dryrun-guard`: S2 Designer/Renderer 补齐 Tabs/Group 加分物料最小闭环: schema parser 支持递归 `Group.fields` 与 `Tabs.tabs[].fields`,全局校验字段 name/LLM target;Renderer 递归渲染容器且答案继续保持 flat answer model;Designer 可添加 Group/Tabs,属性栏用 JSON 编辑子结构,保存时递归剥离 `_draftId` 并把 `export_fields` 展开为叶子字段顺序。S3 dry-run 增加 env-gated task-scoped quota/circuit breaker: `LLM_DRY_RUN_QUOTA_MAX_RUNS`、`LLM_DRY_RUN_CIRCUIT_MAX_FAILURES`、`LLM_DRY_RUN_GUARD_WINDOW_MINUTES`,对 ad-hoc prompt dry-run、单样本 golden dry-run、batch golden dry-run 超限返回 429 且不调用 provider。
- 2026-05-25 `s2-designer-drag-ordering`: Template Designer 增加字段拖拽排序体验:latest 模板的 canvas field 提供 Drag handle,拖拽只按 `_draftId` 重排现有 `fields` 数组,保留选中字段和 Copy/Up/Down 键盘 fallback;历史只读、task mismatch、schema error 等不可编辑状态会禁用拖拽,保存 payload 的 `fields/export_fields` 顺序跟随画布且继续剥离 `_draftId`。
- 2026-05-25 `s2-designer-real-item-preview`: Template Designer 接入真实 task item payload 预览:新增 owner/admin `GET /tasks/:taskId/item-preview` 窄接口,只返回当前 task 第一条 available item 的 `id/externalId/payload` JSON value,不暴露 claimed/status/submission/revision;Designer 自动加载该 payload 并传给 widget preview,ShowItem 默认 path 修正为 `$payload`,切换 task 后晚到 preview response 不污染当前画布。
- 2026-05-25 `s2-designer-field-controls`: Template Designer 增加字段级 Copy/Up/Down 控制和逐字段 validation 提示:复制字段会生成唯一 name 并保持 `_draftId` 不入保存 payload;上/下移调整 `fields` 与 `export_fields` 顺序;重复 name、空 options、长度范围、LLM target 等错误会显示在对应 canvas field 和属性栏,Save 在校验错误时继续禁用。
- 2026-05-25 `s2-designer-route-race-hardening`: 加固 Template Designer route 异步一致性:快速切换 `/owner/tasks/:taskId/templates/:templateId` 时旧模板加载响应不再覆盖当前页面;invalid template id、404/network load failure、task/template mismatch 都会 fail closed 清空旧草稿并禁用编辑;save/fork 请求 resolve 后若 route 已变化,不会再 Toast 或 navigate 回旧模板。
- 2026-05-25 `s2-designer-task-mismatch-hardening`: 加固 Template Designer route/template 归属一致性:加载 `/templates/:templateId` 后校验返回的 `template.taskId` 必须等于 URL 中的 `taskId`;不一致时显示错误、清空草稿并禁止 save/fork,避免手改 URL 或未来跳转 bug 把模板保存到错误 task。
- 2026-05-25 `s3-owner-history-refresh-fix`: 修复 Owner 单条 golden sample dry-run 失败后 history/trend 不刷新的 P2:单样本失败路径现在与成功路径一样重新加载当前 task/filter 的 dry-run history,让失败记录和趋势摘要及时反映后端已持久化的 failed dry-run。
- 2026-05-25 `s2-designer-editing-v1`: 新增 Owner 模板版本入口和 Template Designer 最小编辑闭环:`/owner/tasks/:taskId/templates` 列出版本,`/owner/tasks/:taskId/templates/:templateId` 加载模板;latest 版本支持点击添加 9 个核心 widget、画布选中/删除、编辑 name/label/required 以及 options/length/path/mode/maxFiles/LLM target/prompt 等基础属性,保存时通过既有 `POST /tasks/:taskId/templates` 创建新版本并剥离 `_draftId`;历史版本只读并可 Fork 为新版本。
- 2026-05-25 `s3-provider-retry-backoff`: OpenAI-compatible provider 新增短重试/backoff 配置:支持 `LLM_RETRY_MAX_ATTEMPTS`、`LLM_RETRY_BACKOFF_MS`、`LLM_RETRY_MAX_BACKOFF_MS`,默认最多 2 次且上限 2s;仅网络错误、HTTP 429 和 5xx 会重试,并尊重/clamp `Retry-After`;HTTP 400/401、schema/threshold validation 仍 fail fast,provider error body 继续不进入持久化错误信息。
- 2026-05-25 `s3-reviewer-ai-verdict-display`: Reviewer Queue/Detail 增加 AI 预审结果展示,复用 submission 上已有 `aiVerdict/aiScore`,左侧队列显示 `AI verdict · score`,详情页在人工审核前显示 AI verdict/score 或空状态;未改后端 API。
- 2026-05-25 `s3-owner-dry-run-history-ui`: Owner Dashboard 的 Golden Samples 区域新增 dry-run history/trend 最小可用视图,复用 `GET /tasks/:taskId/ai-dry-runs?golden_sample_id=&limit=` 自动加载最近记录,支持按 sample filter/History 按钮查看单样本历史,用 compact summary 展示 total/matched/mismatch/failed,table 展示 expected/actual verdict、matched、status/error、prompt version 和 finished time,并沿用 task switch stale guard 防止晚到 history 响应污染当前 task。
- 2026-05-25 `s3-golden-sample-batch-dry-run`: 新增 owner/admin batch golden sample dry-run endpoint:`POST /tasks/:taskId/golden-samples/dry-runs`,body 为 `{sample_ids:[...], ai_prompt_id?: number}`;服务端限制单批最多 20 个样本并串行执行,避免 provider 并发突刺;请求中的 sample 必须全部属于当前 task,否则整体 404;单样本 provider/evaluator/config 失败写入对应 failed dry-run 或返回 per-sample failed result,不阻断后续样本;响应返回 `summary` 与每个 sample 的 status/dryRunId/result/matchedExpected/error。
- 2026-05-25 `s3-owner-batch-run-wiring`: Owner Dashboard 的 Run all visible samples 改接后端 batch dry-run endpoint,一次请求返回 partial results 并映射到 result table;保留单样本 Run endpoint;后端 batch 循环新增 `LLM_BATCH_DRY_RUN_DELAY_MS` 可选样本间隔,默认 0,用于按 provider 限制做基础节流。
- 2026-05-25 `s3-ai-dry-run-history-api`: 新增 owner/admin task-scoped dry-run history list API:`GET /tasks/:taskId/ai-dry-runs?golden_sample_id=&limit=`,复用 task ownership 边界,支持按 golden sample 过滤和 limit 上限,返回 dry-run 记录的 prompt version、payload/expected answer snapshot、expected/actual verdict、matched flag、status/result/error 和完成时间,其中 JSON snapshot/result 以 JSON value 返回而不是 escaped string。
- 2026-05-25 `s3-owner-dry-run-raw-json`: 修复 Owner Dashboard ad-hoc AI prompt dry-run 端到端 raw JSON 精度:前端不再 `JSON.parse` 后走 `apiPost`,改为校验后用 `apiPostRawJSON` 发送 textarea 原始 JSON body,配合后端 RawMessage/provider UseNumber 避免大整数在前端、handler 或 provider message 路径被 float64 精度坍塌;同时补 golden sample prompt choice reset 断言和 ad-hoc dry-run missing/null/invalid JSON 回归。
- 2026-05-25 `s3-golden-sample-draft-reset`: 修复 Owner Dashboard golden sample create draft 跨 task 泄漏:切换到不同 task 时重置 payload/expected_answer/expected_verdict/notes/prompt choice,同 task 重复点击仍 no-op;同时将 ad-hoc AI prompt dry-run 的 payload/answer 改为 raw JSON 传递,避免大整数在 handler/provider message 路径被 float64 精度坍塌。
- 2026-05-25 `s3-golden-sample-owner-ui`: 修复 golden sample JSON contract:创建接口用 raw JSON/canonical hash 保留大整数精度,GET/POST 响应用 JSON value 而不是 escaped string;Owner Dashboard 新增 Golden Samples 管理区,支持加载/创建/删除样本、按 active/指定 prompt 绑定、单样本 dry-run、Run all visible samples 和 result table,并沿用 task/action stale guard 防止跨 task 晚到响应污染 UI。
- 2026-05-23 `s3-golden-sample-dry-run-linking`: 新增 golden sample 单样本 dry-run linking: additive migration 扩展 `ai_dry_runs` 记录 `golden_sample_id`、prompt version、输入快照、expected/actual verdict、matched flag 和完成时间,并从 `ai_prompt_configs` 回填历史 prompt version;新增 `POST /tasks/:taskId/golden-samples/:sampleId/dry-run`,复用 owner/admin ownership、sample pinned prompt/task active prompt、prompt allowlist、mock/OpenAI-compatible provider 和 threshold consistency,成功/失败均写 dry-run 记录且 provider failure 只返回/持久化安全错误信息。批量 dry-run 暂缓,避免在结果表 UI 前先固化 partial failure 和路由语义。
- 2026-05-23 `s3-golden-sample-api`: 新增 owner/admin golden sample persistence API:`GET/POST/DELETE /tasks/:taskId/golden-samples`,复用 task ownership 边界;创建时校验 payload/expected_answer/expected_verdict,可选 `ai_prompt_id` 必须属于当前 task,对 canonical payload JSON 计算 sha256 并用 `uk_task_payload_hash` 做同 task 去重,重复 payload 返回 409。
- 2026-05-23 `s3-ai-same-task-guard`: 修复 Owner Dashboard 同一 task 重复点击会让 in-flight action guard 失效的问题:当前已选 task 再点击直接 no-op,只在实际切换不同 task 时递增 action generation;补 deferred dry-run 回归,确认同 task 晚到 response 仍正常应用且 loading 复位。
- 2026-05-23 `s3-ai-review-followups`: 完成 S3 AI review P2 cleanup:Owner Dashboard 的 save prompt / AI review settings / dry-run 都加 task/action guard,切换 task 后晚到的 action response/error 不再污染当前任务 UI;OpenAI-compatible provider HTTP 400/401 error body 不进入 error message;LLM schema/threshold validation error 标记为 non-retryable,worker 遇到后直接 failover 到人工审核避免重复扣费;补 submit invalid active prompt 和 disallowed active model 的 422 HTTP 回归。
- 2026-05-23 `s3-ai-p1-hardening`: 修复 S3 AI review P1 阻塞项:worker 校验 payload 与 idempotency key 一致,所有 claim/finalize/failover 更新绑定 submission/revision/prompt version,非最终失败回写 failed 以便 Asynq 重试;submission submit 事务内锁定并重载 task 后再决定 AI plan,enabled 但 active prompt 缺失/跨 task/模型不允许时明确拒绝提交;LLM verdict 与 score thresholds 不一致会被拒绝;Owner Dashboard 忽略过期 prompt 响应,加载中/失败时禁用保存、启停和 dry-run,失败 dry-run 清空旧结果;provider HTTP error body 不再写入 dry-run/worker error 持久化路径。
- 2026-05-23 `s3-ai-prompt-form-reset`: 修复 Owner Dashboard 跨任务 AI Prompt 表单残留:切换到无 prompt 的任务或 prompt 加载失败时,表单会重置为默认 prompt_template/dimensions/thresholds/空 model,避免把上一任务 prompt 误保存到当前任务。
- 2026-05-23 `s3-ai-review-controls`: 完成任务级 AI review 启用/关闭控制:新增 owner/admin `POST /tasks/:taskId/ai-review-settings`,复用 `loadOwnedTask` 权限边界,启用前校验 active `ai_prompt_id` 且 prompt 属于当前 task,关闭时只更新 `tasks.ai_review_enabled=false` 并保留 prompt history;Owner Dashboard 在 AI Prompt 区块展示当前状态,无 active prompt 时禁用启用动作,有 prompt 时可启用或关闭并本地更新状态;submission flow 回归确认 `ai_review_enabled=false` 即使有 prompt 也跳过 AI。
- 2026-05-23 `s3-ai-review-fixes`: 修复 S3 AI Prompt review 发现的问题:Owner 新 prompt 默认 model 不再硬编码 `mock-model`,由后端按 `LLM_MODEL`/fallback 决定并受 `LLM_ALLOWED_MODELS` 校验;Owner UI 改用 dimensions JSON textarea,重保存保留 `description`/`weight`;AI worker 成功重试写 `succeeded` 时清空旧 `error_msg`。
- 2026-05-23 `s3-ai-product-layer-part1`: 完成 S3 AI 产品层第一段:新增共享 `llmreview` OpenAI-compatible provider,支持 `LLM_PROVIDER/LLM_BASE_URL/LLM_API_KEY/LLM_MODEL/LLM_ALLOWED_MODELS/LLM_TIMEOUT_MS`;worker 从 prompt version 读取 payload/answer/baseline,通过 Function Calling strict schema 取得结构化 verdict/score/dimensions/reason,Go 端严格校验并记录 tokens/latency/raw_response,未配置时保留 deterministic/mock fallback;Owner API 增加 `/tasks/:taskId/ai-prompts` GET/POST 和 `/tasks/:taskId/ai-prompts/:promptId/dry-run`,按 owner/admin + task ownership 校验,POST append-only 创建 version 并同事务更新 `tasks.ai_prompt_id`;Owner 页面增加 AI Prompt 编辑和 dry-run 结构化结果展示。
- 2026-05-23 `p1-ai-edge-hardening`: 修复 P1/P2 边界复查问题:FileUpload 支持打回修改后复用同 submission 历史 revision 已 attached 的文件;AI worker duplicate replay 在 `markRunning` 抢不到 pending/failed 行时 no-op,且 failover 不再污染 succeeded 记录;API 侧新增 AI review sweeper,对超时停留在 `ai_reviewing` 的 pending/running 预审强制转 `human_reviewing` 并写 `ai_fail_max` audit。
- 2026-05-23 `p1-p2-closeout`: 一次性补齐 P1/P2 审查项:FileUpload 在 submit 事务内把答案引用的 `storageKey` 校验并绑定到 `submission_revision_id`,非法 owner/cross-task/重复 key 有测试;前后端 schema 校验对齐 trim 后 name、严格 options、FileUpload `maxFiles`、LLMTrigger `target_field`;Labeler/Reviewer 增加页面级 schema runtime 回归;AI submit 事务创建 pending `ai_reviews` + `ai:review` outbox,API 后台 publisher 投递 Asynq,worker 幂等消费并在成功/失败后流转到 `human_reviewing`。
- 2026-05-22 `p1-safety-defaults`: 补 P1 安全口径:Save/Submit 在 `submission.Save` 事务内用 `FOR UPDATE` 锁住 `task_items` 并二次校验 `claimed_by/status`;并将 `ai_review_enabled` 的初始 schema、GORM 默认值和 003 迁移统一改为安全默认关闭,旧库中未配置 AI prompt 的任务会被迁移为关闭 AI。
- 2026-05-22 `phase2-schema-runtime`: 完成阶段二动态模板运行时闭环:Labeler Plaza 和 Reviewer Queue 改为读取 `bundle.template.schemaJson` 并通过 `SchemaRenderer` 渲染;Reviewer 详情改走 `/reviewer/submissions/:submissionId`;补齐 RichText/JSONEditor/FileUpload/LLMTrigger 真实可交互 widget;前后端 parser/canonical 保存保留 `export_fields` 与 `x-*` 扩展;Vite 对 React/Semi/lottie 做分包并将 lottie 指到 light player,build 不再出现 eval/chunk warning。
- 2026-05-22 `fix(api): harden review and claim transactions`: 完成阶段一 P0 后端并发与越权重构:新增 `task_reviewers` 显式审核授权表;Reviewer Queue/Detail/Review 统一按 owner/admin/task_reviewers 做资源隔离;`review.Apply` 在事务内锁 task/submission 并校验 `human_reviewing`;Claim 在事务内锁 task、抢 item、立即创建 draft submission 并冻结 `template_version`;Save/Submit 在 submission 行锁保护下生成 revision_no。
- 2026-05-22 `0b871d5`: 修复安全和边界问题,包括 ShowItem URL 白名单、reviewer/owner 审核资源边界、upload task 权限与 MIME/content 校验、template/answer body 限制、CORS 显式 origin、submission template_version snapshot。
- 2026-05-22 `92f08ac`: 完成 S2 Day2 SchemaRenderer runtime,支持官方 schema 解析、核心 widget 渲染、答案校验和 renderer 单测。

## 仍需提升

- S7 已补第二官方任务 seed;答辩演示脚本、录屏、AI Coding 过程 PDF、架构 PDF/API 最终校对仍需按最终提交口径产出。
- S5 工程质量 sprint 已完成主目标;生产 compose 目前完成 `config` 级校验,真实 `up --build` 留给有目标域名/端口和部署机资源的发布环境执行。
- S2 Designer 已有模板版本列表、latest 编辑、历史只读/Fork、append/delete/copy/Up-Down/drag order/物料拖拽放置(drag-to-place 插入到落点)、属性面板「基础/校验/联动」三 tab、顶层字段 `visibleWhen`(条件显示)与 `customRule`(自定义校验,`expr-eval`)可视化配置、逐字段 validation、`regex`/`requiredWhen` runtime 校验、真实 item payload 预览、Tabs/Group 最小结构物料、子字段属性栏可视化编辑和 Save as new version;但 Tabs/Group 子字段还不是画布内嵌套拖拽、嵌套子字段尚不能配 visibleWhen/customRule、`visibleWhen.equals` 仅支持字符串值,layout 编辑也仍较基础。
- AI 预审 P1 安全/状态/前端竞态问题已收敛,并补了 P2 action stale guard/provider error/non-retryable validation cleanup;golden sample 后端 persistence API、Owner 管理 UI、batch result table、task-scoped dry-run history API、Owner history/trend/guard 视图、Reviewer AI verdict/score/detail/audit 展示、Reviewer failed/dead AI retry、单样本 durable queued dry-run + polling、同步串行 batch dry-run endpoint、基础 batch delay 配置、provider 429/backoff 短重试、server-side dry-run quota/circuit breaker、AI worker 5xx 熔断、AI 自动 approved、Labeler 3s 自动保存/修订入口和 Reviewer 批量操作已具备。若后续要继续产品化,主要是把 batch dry-run 也异步化、补更完整 trend 图表和真实运营告警。
- FileUpload 已完成 temp→attached 绑定、打回复用、下载授权接口和 temp orphan cleanup 定时清理;但前端文件预览/下载入口仍较基础,还需要 seeded browser smoke 覆盖。
- `task_reviewers` 目前通过 seed 赋予官方任务的 `reviewer1` 权限,Owner 后台的审核员分配 UI/API 还未实现。

## 下一步

- 继续 S7 答辩材料:演示脚本(5/10 分钟)、AI Coding 过程文档、架构图/PDF、API 文档最终校对。
- 推进 S2 后续:补 Tabs/Group 子字段画布内嵌套拖拽、嵌套子字段的 visibleWhen/customRule 配置、`visibleWhen.equals` 非字符串值,以及更完整 layout 编辑。
- 推进 S3 验收:做一次本地 seeded browser smoke,覆盖 Owner 配规则/跑 golden dry-run、Labeler 提交/修订、AI worker、Reviewer 批量审核和规则查看。
- 补 FileUpload 前端预览/下载入口的 seeded browser smoke。
- 若必须严格贴 PLAN.md 技术路线,再单独评估 Formily、TipTap、`packages/schema-spec` 和 `@dnd-kit/core` 迁移;当前实现优先保持既有自写 renderer/designer 稳定。

## 验证记录

- 2026-05-29 Designer visibleWhen/customRule/drag-to-place: `pnpm -F web exec vitest run src/modules/template` 通过(1 file,16 tests:14 既有 + visibleWhen+customRule 往返、物料拖拽插入两条新增);`pnpm -F web exec tsc -b` exit 0;`pnpm -F web build` exit 0(仍有既存 StatsBoard chunk >500KB warning);`pnpm -F web lint` exit 0;`pnpm -F web exec eslint src/modules/template/Designer.tsx` 单独亦 clean。只改 `apps/web/src/modules/template/Designer.tsx` + `Designer.integration.test.tsx`,`renderer/**`、labeler 模块、`Designer.css` 未动。
- 2026-05-29 角色登录/RBAC: `go test ./apps/api/... ./apps/ai-worker/... ./pkg/exporter ./pkg/llmreview -count=1` 通过;`pnpm -F web test -- --pool threads App.test.tsx modules/auth/Login.test.tsx shared/layout/AppLayout.test.tsx modules/reviewer/Queue.test.tsx` 通过(4 files,17 tests);`pnpm -F web lint` 通过;`pnpm -F web build` 通过;`git diff --check` 通过。build 输出仍保留既有 StatsBoard chunk >500KB warning。
- 2026-05-28 S6 browser smoke / style-guide: `pnpm -F web test`（full）通过(14 files,114 tests);`pnpm -F web lint` 通过;`pnpm -F web build` 通过(仍有既存 StatsBoard chunk >500KB warning);`git diff --check` 通过。浏览器:本地 API:8090 + web:5173,chrome-devtools 截图核验 1280/1920(Designer 三栏↔属性下移正确)、console 仅 React Router future-flag warning;smoke 后已 `lsof -ti tcp:8090 tcp:5173 | xargs kill` 释放端口,无残留进程。
- 2026-05-28 S6 a11y/shortcut: `pnpm -F web test`（full）通过(14 files,114 tests,含新增 Labeler Ctrl/Cmd+Enter 提交测试,仍有 jsdom canvas warning);`pnpm -F web lint` 通过(修了 `react-hooks/refs`——ref 改在无依赖 effect 里更新,不在 render 期间写);`pnpm -F web build` 通过(仍有既存 StatsBoard chunk >500KB warning);`git diff --check` 通过。
- 2026-05-28 S6 error copy: `pnpm -F web test`（full）通过(14 files,113 tests,含新增 `ApiError` 5 条单测,仍有 jsdom canvas warning);`pnpm -F web lint` 通过;`pnpm -F web build` 通过(仍有既存 StatsBoard chunk >500KB warning);`git diff --check` 通过。
- 2026-05-28 S5 review fixes: `docker compose --env-file deploy/.env.example -f deploy/docker-compose.prod.yml config` 通过(asynqmon 鉴权变量正确注入 caddy);缺 `ASYNQMON_PASSWORD_HASH` 时 compose 按 `:?` 拒绝渲染;`jq empty docs/LabelHub.postman_collection.json` 通过;`git diff --check` 通过。未跑 Go/web 测试(本次仅改部署模板 + Postman/文档,无代码逻辑改动)。
- 2026-05-28 S6 designer responsive: `pnpm -F web build` 通过(补回缺失的 `Designer.css`,仍有既存 StatsBoard chunk >500KB warning);`pnpm -F web lint` 通过;`pnpm -F web test`（full）通过(13 files,108 tests,含新增嵌套画布预览断言,仍有 jsdom canvas warning);`git diff --check` 通过。响应式断点(1920/1280/窄屏)依赖 CSS media query,jsdom 不渲染布局,留待 Day5 浏览器 smoke 实测。
- 2026-05-28 S6 runtime tabs: `pnpm -F web test -- SchemaRenderer Designer.integration` 通过(13 files,108 tests,仍有 jsdom canvas warning);`pnpm -F web lint` 通过;`pnpm -F web build` 通过(仍有既存 StatsBoard chunk >500KB warning);`git diff --check` 通过。
- 2026-05-28 S6 dense/status surfaces: `pnpm -F web test -- ExportPanel Plaza Queue Dashboard StatePrimitives` 通过;full `pnpm -F web test` 通过(13 files,108 tests,仍有 jsdom canvas warning);`pnpm -F web lint` 通过;`pnpm -F web build` 通过(仍有既存 StatsBoard chunk >500KB warning);`git diff --check` 通过。
- 2026-05-28 S6 state primitives: `pnpm -F web test -- StatePrimitives` 通过(13 files,108 tests,仍有 jsdom canvas warning);`pnpm -F web lint` 通过;`pnpm -F web build` 通过(仍有既存 StatsBoard chunk >500KB warning);`git diff --check` 通过。
- 2026-05-28 S6 plan: 文档-only 变更,未跑测试;前一提交的 S5 全量 Go/web/config 门禁已通过。
- 2026-05-28 S5 deploy/docs: `docker compose --env-file deploy/.env.example -f deploy/docker-compose.prod.yml config` 通过;`make -n dev` 通过;`go test ./apps/api/... ./apps/ai-worker/... ./pkg/exporter ./pkg/llmreview -count=1` 通过;`pnpm -F web test` 通过(12 files,103 tests,仍有 jsdom canvas warning);`pnpm -F web lint` 通过;`pnpm -F web build` 通过(仍有既存 StatsBoard chunk >500KB warning);`pnpm -F web gen:api` 通过;`jq empty docs/LabelHub.postman_collection.json` 通过;`git diff --check` 通过。生产 compose 未在本机执行 `up --build`,避免占用 80/443 和拉取/构建全部镜像。
- 2026-05-28 S5 openapi/error-boundary: `pnpm -F web gen:api` 通过并生成 `schema.d.ts`;`jq empty docs/LabelHub.postman_collection.json` 通过;`pnpm -F web test -- ErrorBoundary SchemaRenderer Designer.integration` 通过(12 files,103 tests);`pnpm -F web lint` 通过;`pnpm -F web build` 通过(仍有既存 StatsBoard chunk >500KB warning)。
- 2026-05-28 S5 frontend strict/roundtrip: `pnpm -F web build` 通过(`strict:true`,仍有既存 StatsBoard chunk >500KB warning);`pnpm -F web test -- SchemaRenderer Designer.integration` 通过(11 files,102 tests;含新增 Renderer/Designer round-trip);`pnpm -F web lint` 通过。
- 2026-05-28 S5 integration main-flow:普通 `go test ./apps/api/... ./apps/ai-worker/... ./pkg/exporter ./pkg/llmreview -count=1` 通过;本机 Colima 环境用 `DOCKER_HOST=unix:///Users/dadadineiyou/.colima/default/docker.sock TESTCONTAINERS_RYUK_DISABLED=true go test -tags=integration ./apps/api/internal/integration -count=1 -v` 通过(真 MySQL+Redis 容器,7.8s);`git diff --check` 通过。
- 2026-05-28 S5 quality day1 partial: targeted `go test -cover ./apps/api/internal/statemachine ./apps/api/internal/service/review ./apps/api/internal/service/outbox ./apps/api/internal/handler ./pkg/exporter` 通过,覆盖率分别为 statemachine 100.0%、review 70.1%、outbox 45.5%、handler 61.4%、exporter 87.8%;targeted `go test -cover ./apps/ai-worker/cmd/worker` 通过,worker 覆盖率 60.6%(含新增 verdict mapping + circuit open failover);targeted `pnpm -F web test -- StatsBoard` 通过(100 tests,含新增 stale retry guard)。
- 2026-05-28 S4 review fixes: `go test -race ./apps/api/... ./apps/ai-worker/... ./pkg/exporter ./pkg/llmreview` 全绿;`go vet` 通过;`pnpm -F web test` 99 通过(新增 stale-error / 轮询会停 / StatsBoard 错误态+重试 / 空维度 / CSV 注入回归);`pnpm -F web lint` 通过;`pnpm -F web build` 通过(VChart 进异步 StatsBoard chunk,eager vendor 降至 412KB);`git diff --check` 干净。
- 2026-05-27 S3 hardening cleanup: targeted `cd apps/api && go test -count=1 ./internal/handler -run 'Upload|DownloadUpload|RespondItem|ReviewerAIPromptActivateRouteIsNotRegistered'` 通过;targeted `cd apps/api && go test -count=1 ./internal/statemachine ./internal/service/submission ./internal/service/aireview ./internal/handler` 通过;targeted `cd apps/ai-worker && go test -count=1 ./cmd/worker -run 'AIWorkerCircuit|HandleAIDryRunCircuitOpenMarksRunFailed'` 通过;full `cd apps/api && go test -count=1 ./...` 通过;full `cd apps/ai-worker && go test -count=1 ./...` 通过;`git diff --check` 通过。
- 2026-05-27 S3 review finding fixes: targeted `cd apps/api && go test -count=1 ./internal/handler -run 'TestRespondItem_ExistingSubmissionUsesTemplateVersion|TestReviewerAIPromptsScopedToAssignedReviewer|TestReviewerAIPromptsRejectsUnassignedReviewer|TestReviewerAIPromptActivateRouteIsNotRegistered|TestReviewerDetailIncludesAIReviewAndAuditLogs'` 通过;targeted `cd apps/ai-worker && go test -count=1 ./cmd/worker -run 'TestHandleAIDryRun|TestAIWorkerCircuit'` 通过;`pnpm -F web test -- Queue` 通过;full `cd apps/api && go test -count=1 ./...` 通过;full `cd apps/ai-worker && go test -count=1 ./...` 通过;`pnpm -F web lint` 通过。
- 2026-05-27 S3 durable dry-run/rule ops: targeted `go test ./apps/api/internal/handler ./apps/ai-worker/cmd/worker` 通过;targeted `pnpm -F web test -- Queue Dashboard` 通过;full `go test ./apps/api/... ./apps/ai-worker/... ./pkg/llmreview` 通过;full `pnpm -F web test` 通过;`pnpm -F web lint` 通过;`pnpm -F web build` 通过(仍有既有 >500k chunk warning);`git diff --check` 通过。
- 2026-05-27 S0-S3 plan gap polish: `docker compose --profile ops -f deploy/docker-compose.yml config` 通过;targeted `go test ./apps/api/internal/handler -run 'TemplateSchema|Template'` 通过;targeted `go test ./apps/ai-worker/cmd/worker ./pkg/llmreview` 通过;CI 等价 Go command `go test ./apps/api/... ./apps/ai-worker/... ./pkg/llmreview` 通过;targeted `pnpm -F web test -- validator parser Plaza` 通过;full `pnpm -F web test` 通过;`pnpm -F web lint` 通过;`pnpm -F web build` 通过。
- 2026-05-26 S3 demo productization closure: targeted `cd apps/api && go test -count=1 ./internal/handler -run 'UpdateTaskBaseline|GoldenSampleDryRunQueuesAsyncRun|BatchReviewAppliesApprove|ReviewerDetailIncludesAIReviewAndAuditLogs'` 通过;targeted `cd apps/ai-worker && go test -count=1 ./cmd/worker -run 'CompleteAutoApprovesPassWhenHumanReviewDisabled|CompleteMovesSubmissionToHumanReviewWithAIVerdict|HandleAIReviewUsesProviderResultAndRecordsUsage|CompleteClearsPreviousErrorMessageOnRetrySuccess'` 通过;targeted `pnpm -F web test -- Dashboard Plaza Queue` 通过;full `cd apps/api && go test -count=1 ./...` 通过;full `cd apps/ai-worker && go test -count=1 ./...` 通过;full `pnpm -F web test` 通过;`pnpm -F web lint` 通过;`pnpm -F web build` 通过;`git diff --check` 通过。
- 2026-05-26 S3 Reviewer rule config entry: targeted `cd apps/api && go test -count=1 ./internal/handler -run 'ReviewerAIPrompts|ReviewerDetailIncludesAIReviewAndAuditLogs'` 通过;targeted `pnpm -F web test -- Queue Dashboard` 通过;full `cd apps/api && go test -count=1 ./...` 通过;full `pnpm -F web test` 通过;`pnpm -F web lint` 通过;`pnpm -F web build` 通过;`git diff --check` 通过。
- 2026-05-26 S3 Reviewer AI detail wiring/retry: targeted `cd apps/api && go test -count=1 ./internal/handler -run 'RetryAIReview|ReviewerDetailIncludesAIReviewAndAuditLogs'` 通过;targeted `pnpm -F web test -- Queue` 通过;full `cd apps/api && go test -count=1 ./...` 通过;`pnpm -F web lint` 通过;`pnpm -F web build` 通过。
- 2026-05-26 S2 Designer nested visual editing: targeted `pnpm -F web test -- Designer.integration.test.tsx SchemaRenderer` 通过;full `pnpm -F web test` 通过;`pnpm -F web lint` 通过;`pnpm -F web build` 通过。
- 2026-05-25 S2 Tabs/Group + S3 dry-run guard: targeted `pnpm -F web test -- SchemaRenderer Designer.integration.test.tsx` 通过;targeted `cd apps/api && go test -count=1 ./internal/handler -run 'AIPromptDryRun|GoldenSample.*DryRun|DryRunGuard'` 通过;full `cd apps/api && go test -count=1 ./...` 通过;`pnpm -F web lint` 通过;`pnpm -F web build` 通过。
- 2026-05-25 S2 Designer drag ordering: targeted `pnpm -F web test -- Designer.integration.test.tsx` 通过;full `pnpm -F web test` 通过;`pnpm -F web lint` 通过;`pnpm -F web build` 通过。未改后端。
- 2026-05-25 S2 Designer task mismatch hardening: targeted `pnpm -F web test -- Designer` 通过;`pnpm -F web build` 通过;`pnpm -F web lint` 通过。
- 2026-05-25 S3 Owner history refresh fix: targeted `pnpm -F web test -- Dashboard` 通过;`pnpm -F web lint` 通过;`pnpm -F web build` 通过。
- 2026-05-25 S2 Designer editing v1: targeted `pnpm -F web test -- src/modules/template/Designer.integration.test.tsx` 通过;full `pnpm -F web test` 通过;`pnpm -F web lint` 通过;`pnpm -F web build` 通过。未改后端。
- 2026-05-25 S3 provider retry/backoff: targeted `cd apps/api && go test -count=1 ./internal/service/aiprompt` 通过;full `cd apps/api && go test -count=1 ./...` 通过;extra `cd apps/ai-worker && go test -count=1 ./...` 通过。
- 2026-05-25 S3 Reviewer AI verdict display: targeted `pnpm -F web test -- Queue` 通过;`pnpm -F web lint` 通过;`pnpm -F web build` 通过。未改后端。
- 2026-05-25 S3 Owner dry-run history UI: targeted `pnpm -F web test -- Dashboard` 通过;`pnpm -F web lint` 通过;`pnpm -F web build` 通过。未改后端。
- 2026-05-25 S3 Owner batch run wiring: targeted `cd apps/api && go test -count=1 ./internal/handler -run 'GoldenSampleBatchDryRun'` 通过;targeted `pnpm -F web test -- Dashboard` 通过;full `cd apps/api && go test -count=1 ./...` 通过;extra `cd apps/ai-worker && go test -count=1 ./...` 通过;`pnpm -F web test` 通过;`pnpm -F web lint` 通过;`pnpm -F web build` 通过。
- 2026-05-25 S3 golden sample batch dry-run: targeted `cd apps/api && go test -count=1 ./internal/handler -run 'GoldenSampleBatchDryRun|GoldenSampleDryRunRecordsMatchedExpected'` 通过;full `cd apps/api && go test -count=1 ./...` 通过;extra `cd apps/ai-worker && go test -count=1 ./...` 通过。未改前端,未跑 web test/lint/build。
- 2026-05-25 S3 AI dry-run history API: targeted `cd apps/api && go test -count=1 ./internal/handler -run 'AIDryRuns'` 通过;full `cd apps/api && go test -count=1 ./...` 通过;extra `cd apps/ai-worker && go test -count=1 ./...` 通过。未改前端,未跑 web test/lint/build。
- 2026-05-25 S3 Owner dry-run raw JSON: targeted `cd apps/api && go test -count=1 ./internal/handler -run 'AIPromptDryRun'` 通过;targeted `pnpm -F web test -- Dashboard` 通过;full `cd apps/api && go test -count=1 ./...` 通过;`cd apps/ai-worker && go test -count=1 ./...` 通过;`pnpm -F web test` 通过;`pnpm -F web lint` 通过;`pnpm -F web build` 通过。
- 2026-05-25 S3 golden sample draft reset: targeted `cd apps/api && go test -count=1 ./internal/handler -run 'AIPromptDryRun|GoldenSample'` 通过;targeted `cd pkg/llmreview && go test -count=1 ./...` 通过;targeted `pnpm -F web test -- Dashboard` 通过;full `cd apps/api && go test -count=1 ./...` 通过;extra `cd apps/ai-worker && go test -count=1 ./...` 通过;`pnpm -F web test` 通过;`pnpm -F web lint` 通过;`pnpm -F web build` 通过。
- 2026-05-25 S3 golden sample owner UI: `cd apps/api && go test -count=1 ./...` 通过;`pnpm -F web test` 通过;`pnpm -F web lint` 通过;`pnpm -F web build` 通过。
- 2026-05-23 S3 golden sample dry-run linking: `cd apps/api && go test -count=1 ./...` 通过;`pnpm -F web test` 通过;`pnpm -F web lint` 通过;`pnpm -F web build` 通过。
- 2026-05-23 S3 golden sample persistence API: `cd apps/api && go test -count=1 ./...` 通过;`pnpm -F web test` 通过;`pnpm -F web lint` 通过;`pnpm -F web build` 通过。
- 2026-05-23 S3 AI same-task guard: `pnpm -F web test` 通过;`pnpm -F web lint` 通过;`pnpm -F web build` 通过。
- 2026-05-23 S3 AI review followups: `cd apps/api && go test -count=1 ./...` 通过;`cd apps/ai-worker && go test -count=1 ./...` 通过;`pnpm -F web test` 通过;`pnpm -F web lint` 通过;`pnpm -F web build` 通过。
- 2026-05-23 S3 AI P1 hardening: `cd apps/api && go test -count=1 ./...` 通过;`cd apps/ai-worker && go test -count=1 ./...` 通过;`pnpm -F web test` 通过;`pnpm -F web lint` 通过;`pnpm -F web build` 通过。
- 2026-05-23 S3 AI prompt form reset: `pnpm -F web test` 通过;`pnpm -F web lint` 通过;`pnpm -F web build` 通过。
- 2026-05-23 S3 AI review controls: `cd apps/api && go test -count=1 ./...` 通过;`cd apps/ai-worker && go test -count=1 ./...` 通过;`pnpm -F web test` 通过;`pnpm -F web lint` 通过;`pnpm -F web build` 通过。
- 2026-05-23 S3 AI review fixes: `cd apps/api && go test -count=1 ./...` 通过;`cd apps/ai-worker && go test -count=1 ./...` 通过;`pnpm -F web test` 通过;`pnpm -F web lint` 通过;`pnpm -F web build` 通过。
- 2026-05-23 S3 AI product layer part1: `cd apps/api && go test -count=1 ./...` 通过;`cd apps/ai-worker && go test -count=1 ./...` 通过;`pnpm -F web test` 通过;`pnpm -F web lint` 通过;`pnpm -F web build` 通过。
- 2026-05-23 P1/P2 edge hardening: `cd apps/api && go test -count=1 ./...` 通过;`cd apps/ai-worker && go test -count=1 ./...` 通过;`pnpm -F web test` 通过;`pnpm -F web lint` 通过;`pnpm -F web build` 通过。
- 2026-05-23 P1/P2 closeout: `cd apps/api && go test -count=1 ./...` 通过;`cd apps/ai-worker && go test -count=1 ./...` 通过;`pnpm -F web test` 通过;`pnpm -F web lint` 通过;`pnpm -F web build` 通过。
- 2026-05-22 P1: `cd apps/api && go test ./...` 通过;`make seed` 通过;本地 MySQL 确认 `tasks.ai_review_enabled` 默认值为 `0`,官方 seed 任务 AI 为关闭。
- 2026-05-22 阶段二: `cd apps/api && go test ./...` 通过;`pnpm -F web test` 通过;`pnpm -F web lint` 通过;`pnpm -F web build` 通过且无 lottie eval/chunk size warning。
- 2026-05-22 阶段一: `go test ./...` 通过;`make seed` 通过并确认本地 MySQL `task_reviewers` 中官方任务已分配 `reviewer1`。
